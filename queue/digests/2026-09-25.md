# Daily digest 2026-09-25

## Summary

**The most important thing today: every gap from yesterday's Premiere report is closed, and the ninety second cut now takes 19 actions against Premiere's 20.**
Yesterday the loop measured the video editor against Premiere and filed ten gaps. Overnight it closed all the ones that did not need your answer, then re-ran the count (`count-the-ninety-second-cut-against-premiere-aga`, `fa1f1ea4`). A 5:19 talk cut to exactly 1:30 at Next defaults took 19 actions. Premiere takes 20 with its default keys. Taking out one marked stretch takes 3 presses (I, O, apostrophe). After that the loop moved on to matching your video mocks: saving an edit, the Library, the panel, sound fades and motion paths.

This morning you filed the first report from real use: auto captions got words wrong, and there was no way to fix one word or drag it. That task (`fix-a-caption-one-word-at-a-time-on-the-canvas-a`) is first in the queue.

**Completed: 20 tasks** (one of them, the Premiere measurement, was already reported yesterday).

Closing the Premiere gaps:
- `the-timeline-ruler-names-the-right-time-when-zoo`: zoomed in, every ruler number sits over its moment. It used to be three seconds off at 4x.
- `premiere-s-timeline-keys-work-the-moment-a-recor`: J, K, L, I, O, M and B work as soon as a recording opens, with no click on the timeline first.
- `cutting-a-stretch-out-of-a-recording-takes-its-c`: cutting a piece out also takes out its captions, and moves up the titles and sounds after it.
- `marking-an-in-and-an-out-and-pressing-one-key-ta`: Extract (apostrophe) and Lift (semicolon) take a marked stretch out of every track.
- `a-long-captioned-recording-edits-without-freezin`: on a five minute captioned talk, keys cost 20 to 47 ms, down from 170 to 485 ms.
- `q-and-w-trim-the-clip-under-the-playhead-up-to-i`: Q and W ripple trim up to the playhead.
- `a-title-typed-over-a-video-is-readable-without-r`: press T over a video and the title comes out white, bold and large, with a soft shadow.
- `one-key-puts-the-usual-transition-on-the-cut-at`: Command T puts the default transition on the cut. Command D stays Deselect, the Photoshop key.
- `the-export-sheet-lets-you-pick-the-size-of-the-v`: the Export sheet has a Size row (Full, 1080p, 720p), and its size estimate is honest now.
- `s-switches-timeline-snapping-off-and-on`: S, a magnet button and a menu row switch timeline snapping off and on.
- `count-the-ninety-second-cut-against-premiere-aga`: the re-count described above.

Matching the video mocks:
- `a-video-edit-can-be-saved-as-a-project-and-opene`: File > Save As keeps a whole edit as a project, and it opens again exactly as it was saved.
- `recordings-and-sounds-can-go-into-the-library-wi`: Import Media, the plus on the Library and a drop on the shelf fill the Library without touching the timeline.
- `on-a-video-the-timeline-is-the-layer-list-and-th`: on a video the panel no longer repeats the timeline as a Layers list.
- `the-properties-pane-opens-on-the-clip-line-and-l`: the panel opens on one line naming the clip, then lists only what is animating.
- `a-sound-shows-its-fade-in-and-fade-out-as-the-mo`: a Fades section with In, Out and Curve. The fades show on the sound's bar.
- `a-moving-layer-draws-its-path-on-the-canvas-and`: a moving layer draws its route. Drag the middle of the route to bend it into an arc.
- `the-video-guides-teach-the-editor-that-exists`: six video guides now teach the real editor with Premiere's keys, at Next defaults.

The loop itself:
- `a-runner-never-stops-to-wait-for-its-own-test-ru`: runners had been ending their turn to wait for their own test run. That happened twice more in this window, both before the fix at 03:03Z, and not since.

**Blocked on you: seven cards, none answered in the last day.** One matters more than the rest. **What Command S does on a video edit** (`command-s-on-a-video-edit-does-what-the-user-pic`) now holds up two things:
- Retiring the old recording window. That retirement is what finally removes the trim bar floating over every recording on open, which is the rough edge the audits repeat most (see Design review).
- Command S itself, which is still dimmed on a fresh recording. Save As works.

The other six cards:
- Whether a video shows the Library without being asked.
- Whether clips show small pictures along them.
- Where a keyed value's curve opens.
- Which pointer and click feature a recording gets first.
- How long a panel line may be.
- How many words an Experiments switch gets.

**Notable commits** (26 in 24 hours):
- `0451c6b3`: the keys work on open.
- `c21c0fc1`: Extract and Lift.
- `0c3e3a55`: no more freezing on a long captioned talk.
- `3460bbdc`: saving a video edit.
- `256e8b0b` and `b009ac43`: the new video panel.
- `a0fc5bbb`: motion paths.
- `fa1f1ea4`: the re-count.

**Sweeps.** There were two full-set runs, twelve hours apart:
- 582 of 599 walks passed at 21:18Z.
- 612 of 617 passed at 09:34Z.

The later run said the app quit part way through `tutorial-trim-a-recording-walk` and left no crash report. I ran that walk on its own this morning and it passed all 7 steps, so no crash task is filed. It is logged on the standing walk task and on the trim task that owns the walk. The latest rotating check passed 50 of 50.

**Where the day went** (`loop-day.mjs --hours 24`):
- Tasks: 900m (62.5%).
- Sweeps: 286m (19.8%, 2 full-set runs).
- Rotating checks: 220m (15.3%, 17 of them).
- Between: 34m (2.4%).

Building was the majority of the day, down from 72.9% yesterday. The difference went into rotating checks: 17 today against 7 yesterday, because more tasks landed code. Two full-set runs is the floor working as intended. No run went blind; the last blind one was 2026-09-22.

## Reflections

- **Flaky tests are now the loop's biggest cost that is not a feature.** Two tests that weigh an exported file gave different answers from run to run. In the 36 hours to this morning they failed in the full test run about a dozen times, across nearly every runner. Once, 4 of 6 runs in a row were red. Each red run costs the runner a 220 second re-run before it can commit, and it teaches everyone to shrug at red. I moved `the-export-size-test-fails-about-one-run-in-thre` to p1, right after your caption ask. Two other flaky tests stay at p2:
  - `a-switched-off-motion-test-passes-whichever-rend`
  - `the-icon-preview-timing-test-goes-red-again-when`
- **The Premiere comparison should become a timed run, not just a count of presses.** The objective says "in less time than in Premiere", and the loop has only counted presses. Two things on the list are still slower than Premiere:
  - Exporting five minutes takes 342 seconds, longer than the video plays (`a-five-minute-recording-exports-in-less-time-tha`).
  - Picking a caption stalls for 120 to 190 ms (`picking-or-retyping-a-caption-answers-at-once`).

  When the manager next re-runs `docs/design/video-vs-premiere.md`, it should time the whole session, from open to exported file.
- **Runners are still filing test tasks by accident.** There were two more today (`lib-tiles-dup-check`, `x-6`), eight in two days. `trying-out-addjson-never-files-a-real-task` stays in p2 with the new cases logged. The fix is small.
- **Each side-by-side check against your mocks files one or two new p1 tasks, and that is fine.** It is why p1 grew from 15 to 18 even though 19 tasks were done. The new ones all say how the app differs from your mocks:
  - The dock headers.
  - The sound's channel strip.
  - Between the keys.
  - The window title line.
  - Library tile width.
  - A new empty video.

  That is the "mocks are the floor" rule doing its job. None of them cleared the bar to drop.

## Design review

**Is the IA sound? Mostly, with one real conflict and one known bend.**
- *Two of your mocks disagree about the panel.* `video.html` puts a moving layer's path in its Animating list. `video-move-wt.html` gives it a Between the keys section with Curve and Path (`queue/audits/2026-09-25-motion-path.json`). The runner followed the main mock and filed the other rather than picking a side. The follow-up, `a-moving-layer-shows-between-the-keys-in-propert`, is told to open a card if both cannot be built.
- *The focus ring round the timeline is still not in the mock.* It tells you which part has the keys (`2026-09-24-timeline-keys-on-open.json`). This is the known bend from yesterday, and it is now smaller: the keys work from the moment a recording opens.
- *The canvas lost its settings row on a video* (`2026-09-24-timeline-is-the-layer-list.json`). Canvas Size is in the Image menu and on a right-click over bare canvas. On a recording there is almost never bare canvas, so the menu is the only way. This is noted, not filed; it may become a finding once someone wants a square export.

**Missing components or guidance.**
- Nothing was built twice this cycle.
- Two gaps from your mocks run across the whole dock, and each has one owning task:
  - The group header: a small caps title, a count chip and a three dot menu (`the-dock-headers-match-the-mock-small-caps-title`).
  - The Library tile grid (`library-tiles-are-as-wide-as-the-mock-draws-them`).
- A blind spot keeps showing in the audits, as it did yesterday: the walks cannot use the menu bar, hover the ruler or drag with a real pointer. Three audits worked around it: `default-transition-key`, `ripple-trim-to-playhead` and `extract-and-lift`. The drags and menu rows below are worth trying by hand.

**Recurring UX issues.**
1. *The trim bar floating over the picture on open.* It appeared in three places:
   - The `timeline-keys-on-open` audit.
   - The `video-guides` audit, which says it floats through guides that never use it.
   - The first picture of the cut guide.

   The real fix is `trim-becomes-a-tool-and-the-recording-window-ret`, which waits on the Command S card. Until then `the-trim-bar-a-recording-opens-with-stays-clear` is the stopgap.
2. *Words cut off with "...".* There are new cases:
   - The captions button now reads "Auto capti..." (it used to read "Write Ag...").
   - The new clip line reads "Tutorial Sam...".
   - Library tiles read "Tutor....mp4".

   The owner, `the-captions-section-says-every-button-and-colou`, is at the top of p2 but has not run, because p1 is full. I logged the new cases on it and handed the tile names to the tile task.
3. *Menus open at the foot of the screen and scroll after three rows.* This was seen on the clip menu and the ruler menu (`2026-09-24-ripple-trim-to-playhead.json`). Filed as `a-right-click-on-a-timeline-clip-shows-the-whole`.
4. *Menu case.* Your mock writes menus in sentence case ("Apply default transition"). The app uses Title Case, as macOS menus do (`2026-09-24-default-transition-key.json`). This happened once, and the macOS convention is right for a menu bar, so it is not filed.

**Rule scrubs: one queued.** Rule D10 in `shared/UX-PATTERNS.md` still says the panel beside a video's timeline lists every value that *could* be animated, and it names `video.html` as the model for that. Your restored `video.html` lists only what *is* animating, with an Animate a property button for the rest, and that is what shipped in `b009ac43`. A runner who trusts D10 would rebuild the old list and fight your mock. Filed as `the-design-rules-say-what-the-video-mock-says-ab` (p2): change the rule, never the mock.

**Judging the focus as a user.** From the audits alone, a Premiere editor can open a recording and cut it to ninety seconds with the keys they already know, all at Next defaults. Here is what would trip them, in the order they would hit it:
1. **The first frame is blurred** until the playhead moves. I confirmed this from the cut guide's first picture. It is now p1, third.
2. **The trim bar sits over the picture** and over the bottom caption.
3. **Command S is dimmed** on a fresh recording. Only Save As works.
4. **A second clip's sound draws as a flat line.** I confirmed this from the second-clip guide's picture. It is now p1, fourth.
5. **A wrong caption word cannot be fixed on its own.** This is your report, and it is first.
6. **Exporting five minutes takes longer than five minutes.**

Against your mocks, the panel is much closer today (the clip line, Animating only, Fades), but the dock headers, the channel strip and the tile grid still differ. All three are p1.

## Ready to try

Everything below is at Next defaults, with no switch turned on. Open any screen recording, or the practice one in Help > Tutorials > Video.

**Cut it the Premiere way** (`queue/audits/2026-09-24-extract-and-lift.json`, `2026-09-24-ripple-trim-to-playhead.json`, `2026-09-24-timeline-keys-on-open.json`).
- Press L to play and K to stop. No click on the timeline is needed first.
- Press I, then O, then apostrophe, and the stretch is gone from every track, captions included.
- Park the playhead and press Q or W to trim the clip up to it.
- Press Command T for a cross dissolve on the cut, and S to switch snapping off.

**Keep an edit** (`queue/audits/2026-09-25-video-project.json`). File > Save As writes a project. Close it, open it, and every track, cut, title, caption and sound is back. Command S on a never-saved recording is still dimmed; that is the card waiting on you.

**Fill the Library first** (`queue/audits/2026-09-25-library-import.json`). Use File > Import Media (Shift Command I), or the plus on the Library header. Tiles land on the shelf and nothing goes on the timeline until you drag one there. The Library still needs View > Library to show (another card), and the names are cut short (p1).

**The new panel on a video** (`queue/audits/2026-09-25-properties-pane.json`, `2026-09-25-sound-fades.json`). Pick a clip, and the panel opens on one line with its name, in, out, length and speed. Below that, Animating lists only what moves, and Animate a property finds the rest. Pick a sound and type a fade into the Fades In box.

**Bend a move into an arc** (`queue/audits/2026-09-25-motion-path.json`). Key a title at two places. Its route shows as a dashed line; drag the square in the middle and the move curves. Please try this drag yourself: the walk could not make it with a real pointer.

**The video guides** (`queue/audits/2026-09-25-video-guides.json`). Help > Tutorials > Video has six guides: cut, second clip, moving title, transition, captions and export. Expect the blurred first frame on the first card of each.

## Triage review

Open tasks stayed at 54. By priority, before and after:
- p0: 0 to 0.
- p1: 15 to 18.
- p2: 33 to 31.
- p3: 6 to 5.

**Evidence checks, then moves to p1: three.**
- `the-export-size-test-fails-about-one-run-in-thre` (p2 to p1, second). Its log holds about a dozen sightings in 36 hours, from nearly every runner. It is the most expensive thing in the loop that is not a feature.
- `a-recording-opens-on-a-sharp-first-frame` (p2 to p1, third). I looked at the picture its notes name, `tutorial-cut-a-recording-down-walk/1-a-clip-on-v1-sc.png`: the card at 0:00 is visibly blurred. Confirmed. Something broken that everyone sees goes ahead of the mock matching.
- `a-clip-brought-onto-the-timeline-shows-its-sound` (p2 to p1, fourth). I looked at `tutorial-add-a-second-clip-walk/4-where-they-meet-sc.png`: the b-roll's stretch of the Audio track is a flat line. Confirmed.
- The only new p1 bug-like task filed by the loop, `library-tiles-are-as-wide-as-the-mock-draws-them`, also checked out: the code says 68 points, and the walk picture shows "Tutor....mp4". It keeps its place.
- The tasks created as user (your caption report) need no check.

**Dropped: one.**
- `a-screenshot-separated-before-today-only-has-to` (p3), under the follow-up bar. It affects only files separated before 2026-09-17, and costs a quiet background read on open. Nobody would see anything wrong. It is still searchable with `search --all` if an old file ever visibly stalls.

**Filed: one.**
- `the-design-rules-say-what-the-video-mock-says-ab` (p2), the rule D10 scrub above.

**Merged: none.** I looked hard at two overlaps and kept both pairs apart:
- The Library tiles against the cut-off labels task. The tiles need a new grid, not a shorter label, so the tile names moved to the tile task.
- The captions safe-area bar against your word-editing ask. Both build on the same track, so the bar now waits on your task.

**Grooming.**
- Your caption task's goal is rewritten in plain words. Your quote and the code pointers moved to its notes, word for word.
- The dock headers goal no longer names a file.
- 14 findings were logged onto the tasks that own them. The main ones:
  - The new cut-off labels.
  - The crash walk that did not reproduce.
  - Command D being settled.
  - The two new stray tasks.
  - The panel height task being unblocked.
  - Word picks needing to stay under 50 ms.

**Order.**
- **p1**: your caption ask first. Then the flaky export test, the blurred first frame and the flat waveform. Then the Command S, trim and Library card tasks at their places, then the Premiere and mock-parity work in the order it was already in.
- **p2**: leads with the cut-off labels, the flaky motion test, the standing walk task, caption pick speed and the new rule scrub. Icon and component work come after the video follow-ups, and the four card-blocked tasks are last.
- Every priority is renumbered in steps of 10.

**Health:**
- `guard` reset nothing.
- `leftovers.mjs list` is empty. The two records from yesterday were handed back to their runners, and both tasks finished.
- `objectives-check` prints nothing.
- Every open task has an epic, a plain goal and at least one acceptance item.
- No title encodes a status.
