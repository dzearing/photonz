# Daily digest 2026-08-22

## Summary
The big development today: the unmanned build loop is now real. Commit 32b23b8 landed the design dashboard, the file-based task queue, and the go loop, and the loop started running tonight. The queue was seeded with six tasks (two high, three normal, one low), all centered on the design study mocks and the Measure/redline direction for the Next release.

Also in the last 24 hours:
- v0.14.0 was released (c382ae6, e139107).
- The video-trim-save branch merged: saving a video now commits the trim to the stored recording, history tiles gained Show in Finder, and pin-to-screen was removed (e501cef).
- The Experiments system merged: Current and Next releases live in one binary, forked by folder, with per-release feature flags (e426ee9).
- A run of measure tool fixes landed: the caliper got three colors, effects join the measure tool's memory, and the caliper stopped being two things.

No queue tasks completed yet and none are blocked; the queue is hours old and the loop has not claimed its first task. No decisions were opened or resolved.

One wrinkle worth knowing: the loop started twice tonight, 42 seconds apart (pids 56941 and 59762). It sorted itself out, but it exposed that the loop has no double-start guard. A task is now queued for that.

## Reflections
- Queued: go-loop-refuses-to-double-start (p2). Two loops racing on task claims would corrupt the queue's one-runner assumption, so the loop should check for a live pid in status.json before starting. This came straight from tonight's history log.
- The seed tasks lean heavily on mock audits. That is the right foundation-first order, but the queue should soon grow tasks that move real app code for the Next release. The measure spec task (spec-the-measure-and-redline-tools-for-the-next, p1) is the bridge: once it lands, its decisions should spawn implementation tasks. Not queueing those now; the spec should drive them so we build validated features, not speculative ones.
- Process idea, not yet queued: the digest currently reads history.jsonl and git log by hand. If the loop grows, a `queue.mjs digest-data` command that emits the last 24 hours of events and task deltas as JSON would make this pass faster and less error prone. Holding off until a few digests show what data actually matters.
- Every seeded task had an empty acceptance array with criteria buried in prose. Seeding tasks through `queue.mjs add` cannot set acceptance items, which is why. If that field matters to runners, `add` should accept them; otherwise the field should go away.

## Triage review
Open tasks before: 6 (p1: 2, p2: 3, p3: 1). After: 7 (p1: 2, p2: 4, p3: 1).

- Merged: none. The six seed tasks are distinct. The closest overlap was the clickthrough sweep re-auditing capture-wt, which the p1 harden task already owns; fixed by scoping instead of merging.
- Priority moves: none. The two p1 tasks match the primary real-world use (UX redlining of screenshots), the audits sit correctly at p2, and the icon sweep at p3.
- Resets: guard pass found no stuck in_progress tasks.
- Groomed: all six tasks got concrete acceptance items lifted out of their prose notes into the acceptance array, so any runner can tell when it is done. The sweep task's notes now say to skip capture-wt. All referenced files (capture-wt.html, dashboard.html, study-tasks.json, AGENTS.md, UX-PATTERNS.md, icons.mjs) were verified to exist.
- Queued: go-loop-refuses-to-double-start (p2), from the double loop_started events in tonight's history.
