{
  "id": "a-walkthrough-framework-guides-are-data-and-they",
  "title": "A walkthrough framework: guides are data, and they point at real controls",
  "goal": "Build the machinery every tutorial will stand on, and prove it with one real guide. A guide is described as data - a list of steps, each naming a control to point at, what to say about it, and what the person has to do to move on - and the app draws a callout with a pointer aimed at that control, following it as the window moves or resizes. Adding a guide later means adding data, not writing UI. Ship it with one guide working end to end, Take the Tour, so the framework is proven rather than asserted.",
  "epic": "ui-building",
  "priority": "p1-high",
  "seq": 20,
  "status": "done",
  "release": "next",
  "area": "app",
  "created": "2026-09-12T15:17:24.150Z",
  "updated": "2026-09-12T21:18:18.486Z",
  "deps": [],
  "blockedBy": [],
  "notes": "This is the foundation task: nothing else in the tutorials set should start before it lands, and the guide tasks all depend on it.\n\nReuse what is already there rather than inventing: the app already draws pointer-and-callout chrome for the zoom callout's leader lines (ZoomCalloutOverlayRasterizer, reached from Sources/PhotonzRender/DocumentRenderer.swift:453) and has an overlay family for toasts and popovers (see the overlay work under interaction-foundations). Look at both before writing a third idiom, and say in the audit which one this is built on and why.\n\nThe clickthrough mocks are the visual reference the user pointed at: docs/design/mocks/pages (92 pages) and the shared component library at docs/design/mocks/shared/components (tooltip.js, overlays.css, app-patterns.css, redline.css). Conform to docs/design/mocks/shared/UX-PATTERNS.md rather than inventing a callout look.\n\nKeep the step's advance condition HONEST. A step that says \"now drag a handle\" and advances on a timer is a lie the person will notice. Wire the waiting kind to real editor events, and where an event does not exist yet, say so in the audit and file it rather than faking it.\n\nOne more thing worth getting right early because it is expensive later: a guide's copy is product copy, so it obeys the repo's copy rules - no em dashes, no mention of the tooling that built it, short plain human sentences.\n\n---\nSHARED CONTEXT for every tutorials task (asked for by the user on 2026-09-12):\n\nThe user's words: \"I would like a help > tutorials > * set of guides which walks you through basics of various features. Maybe a nice getting started tutorial, maybe a 'building components' walkthrough, etc. ... And maybe a first run experience which lets you get an intro to the app, or just gogogo. Use the design clickthroughs as inspiration, where you have pointers and can point to various parts of the application. Build a nice framework for defining these walkthroughs so that we can extend it over time. I'd like the tutorials system to be well organized and not just a long list of things.\"\n\nThe agreed shape (settled in the intake conversation, 2026-09-12):\n- TRACKS, not a flat list. Help > Tutorials holds \"Take the Tour\" plus one SUBMENU per track, and a Tutorials window is the hub. The tracks are: Basics, Redlining, Looks, Colours & Styles, Building UI, Components, Video.\n- A guide is DATA, not code: a Codable value in PhotonzCore describing steps. Adding a guide means adding data to the catalogue, and the menu, the window and the progress list all pick it up with no UI edited. That is the whole point of the framework and the thing that makes it extensible.\n- Steps POINT AT REAL UI, clickthrough-style: a callout with a pointer aimed at a named control, not a page of prose.\n- Design doc: docs/design/tutorials.md is the source of truth for the framework and the track list, written by the foundation task and extended by the rest.\n\nTHE TRAP that will waste a runner's day if it is not read first: the app already has a registry of named UI targets, the playtest harness (playtestControl / playtestField / steady names, Sources/Photonz/Playtest/PanelTarget.swift). It looks like exactly the right thing to reuse and it is NOT USABLE here: it is compiled out of the shipping build (Scripts/build-app.sh defines PHOTONZ_PLAYTEST for the dev and probe variants only, see docs/design/playtest-harness.md, \"Who can run it\"), and PanelTarget.swift:141 onwards is the no-op stub release actually gets. Tutorials ship to users, so the anchor registry has to be its own thing that is compiled IN for release. Model it on the playtest one - especially the STEADY NAME idea at PanelTarget.swift:126-138, a name taken off the model rather than off the copy, so rewording a label cannot break a guide - but do not depend on it.\n\nExisting surfaces a tutorial entry point must fit with rather than duplicate:\n- The Welcome window (Sources/Photonz/Welcome/WelcomeController.swift), first-run permissions setup, re-reachable from the menu-bar menu as \"Welcome & Permissions...\". It already owns first launch.\n- The empty-editor onboarding card (Sources/Photonz/EditorView.swift:590-615): open a file, capture a rectangle, paste an image, blank canvas.\n- There is NO Help menu in the app today: EditorCommands.swift declares Capture, Image, Video, Layer and the standard groups, and macOS supplies the default Help menu. Help > Tutorials has to be created.\n\nQueue-driven app work targets the NEXT release unless the task says otherwise (CLAUDE.md), and a Current change is not finished until Next has it.",
  "acceptance": [
    "A guide is a Codable value in PhotonzCore with tests: a track, a title, an estimated time, and ordered steps; each step names the control it points at, its copy, which side the callout sits on, and how it advances",
    "A named-anchor registry that is COMPILED INTO THE RELEASE BUILD: a control can be tagged with a stable name and the overlay can ask where it is on screen right now",
    "Anchor names are steady: they come off the model, not off the visible words, so rewording a label cannot break a guide",
    "The callout draws over the app with a pointer at the anchor, and follows it when the window is resized, the panel is scrolled or the layout changes; it never covers the control it is pointing at",
    "Two ways to advance, both working: a Next button, and a step that waits for the person to actually do the thing, then moves on by itself",
    "Back, Skip and close all work at every step, the app is never blocked or modal while a guide runs, and quitting mid-guide loses nothing",
    "A guide can open its own sample document so it never depends on what the person has open, and it never edits their work without saying so first",
    "Take the Tour runs start to finish in the real app, pointing at real controls, and is captured in the audit as a sequence of screenshots",
    "A guide that names an anchor which does not exist fails a test rather than failing a person: a check walks every guide in the catalogue and asserts every anchor resolves",
    "docs/design/tutorials.md written: the data model, the anchor contract, how to add a guide, the track list, and what the framework deliberately does not do",
    "Audit under queue/audits/ with the tour captured step by step, and anything rough filed as a follow-up"
  ],
  "log": [
    {
      "t": "2026-09-12T15:17:24.152Z",
      "note": "created (user)"
    },
    {
      "t": "2026-09-12T15:17:24.214Z",
      "note": "seq 270 -> 20"
    },
    {
      "t": "2026-09-12T20:40:51.412Z",
      "note": "claimed by go loop"
    },
    {
      "t": "2026-09-12T20:42:50.044659Z",
      "note": "Adversarial review of the idea, before building.\n\nWhat the mock proposes (docs/design/mocks/shared/components/walkthrough.css, the OPERATED WALKTHROUGH block): one app screen that is driven, a .wt-cue ring pinned over the real control with a pulse and a pill label, and a .wt-panel below the screen carrying a three line caption (Click X / in Y / Result: Z) plus dots, Reset, Back, Next.\n\nWhat does not survive contact with a real window:\n1. The caption panel sits BELOW the app screenshot on a web page. A real window has no below. So the caption has to become a floating card near the control, which means it needs a beak to stay attached (UX-PATTERNS D14, rule 3) and placement that never covers its subject (D14, rule 1).\n2. The three line grammar is for a reader of a page. On screen the ring is already saying which control and where, so Click and in are redundant furniture. Cut to a title and one short sentence, plus N of M.\n3. Reset is a web affordance. Dropped. Back, Skip and close are what a person reaches for.\n4. Clickable dots promise you can jump to step 5, and step 5 may depend on something step 3 made. Replaced with a plain 3 of 7, which promises nothing we cannot keep.\n5. A scrim would make the app modal, and the whole point is you are driving the real app. No scrim, panel ignores the mouse.\n\nWhat the mock assumes the app cannot deliver: a control that is not on screen. The panel can be collapsed, the Library can be hidden, a tool can be behind the more affordance, a feature can be flagged off. So a step needs a way to make its target visible before pointing at it, and it must never be allowed to do the thing the step is asking for (that is the timer lie the task warns about). Hence prepare actions that only reveal, never act.\n\nShortest version that delivers the same value: guide data + anchor registry + one callout that follows + two advance kinds + one guide. Dropped from this task: the Tutorials hub window and the track submenus (already their own task, seq 21), first run (seq 22), and the six tracks (seq 23 to 29). Help gets exactly one row, Take the Tour, and task 21 grows it."
    },
    {
      "t": "2026-09-12T21:18:18.441891Z",
      "note": "Built and verified. What landed:\n\nPhotonzCore (pure, 36 new tests): Tutorials.swift (anchors, steps, tracks, guides, catalogue, the run state machine, progress, the copy rules and the catalogue check), TutorialGuides.swift (Take the Tour, and the sample screen it opens), TutorialCallout.swift (placement, pure geometry).\n\nApp, Sources/Photonz/Tutorials/: TutorialAnchorRegistry (compiled into release, unlike the playtest one), TutorialController (two child panels, a 30Hz follow, progress), TutorialCalloutView (the ring and the card), TutorialLauncher. Plus a Help menu that did not exist, behind next-tutorials.\n\nThree real bugs found by running it rather than by reading it:\n1. Moving from a step whose control is on screen to one whose control is not left the previous step's callout up unchanged, because 'no anchor' and 'no anchor yet' looked the same to the skip-if-unchanged check. Fixed by comparing the step being drawn.\n2. The Measure and Crop buttons are mode buttons and the family slots are group buttons, so none of them went through the shared toolButton funnel and none had a name. That is what surfaced bug 1.\n3. NSView.visibleRect on a SwiftUI hosted view answers for the hosting view around it, not for the marker: reading it put a ring round the whole panel instead of round one section. Clipping the marker's rect to the window instead does what was wanted.\n\nAnd one design gap found the same way: the docked panel is routinely taller than the window, so a step pointing at Position & Size found it below the fold. Added TutorialPrep.revealTarget. The first attempt asked the panel through an observed flag and the panel only acted on it when it happened to redraw for some other reason, which is a second thing that would have shipped looking fine; it now scrolls the marker into view through AppKit directly."
    },
    {
      "t": "2026-09-12T21:18:18.441891Z",
      "note": "Adversarial review of the BUILT thing, on the real app (Scripts/playtest/tutorial-tour-walk.json, six captures with the Screen Recording grant, plus tutorial-resume-walk.json).\n\nWhat is good: you keep driving the app the whole time, the two waiting steps really do wait (pressing I and clicking a layer row are what move them), the ring and the card follow the Layers list when the panel is scrolled, and closing part way and starting again puts you back on step three.\n\nWhat is clumsy, and is filed rather than hidden:\n- The ring is one fixed corner radius, so on the pill shaped tool bar it does not match the shape it is round. Filed p2.\n- A tool sharing a slot with others cannot be pointed at while it is hidden. Filed p2.\n- The fallback when a control is genuinely absent is a card in the middle of the window with no ring. Honest, but a jump. The real answer is the queued live check (seq 30), which is why this one is a fallback and not a feature.\n\nVerified acceptance, item by item: Codable guide with tests (36 tests, Scripts/test.sh green at 5781); anchor registry compiled into release (no PHOTONZ_PLAYTEST anywhere in Sources/Photonz/Tutorials); steady names (tool.measure off Tool.rawValue, panel.layers off the section id, tested); the callout follows a scroll and never covers its control (tested as geometry, shown in capture 04); both advance kinds working in the real app (captures 02 and 03 either side of pressing I); Back, Skip and close at every step, nothing modal (the walk presses real controls with the callout up); the guide opens its own sample window and the window is clean so closing it prompts nothing; the tour runs start to finish; the catalogue check is a unit test; docs/design/tutorials.md written; audit at queue/audits/2026-09-12-tutorials.json with six real captures.\n\nAsked for a sweep, because inert tutorial anchors now hang on shared editor chrome. The panel walks (18) and tool walks (15) are green."
    },
    {
      "t": "2026-09-12T21:18:18.484Z",
      "note": "Guides are data in PhotonzCore with 36 tests; a named-anchor registry compiled into the release build; a ring on the control and a card beside it that follows it; Take the Tour running start to finish in the real app behind next-tutorials, with a Help menu that did not exist before. Verified by Scripts/test.sh (5781 green), Scripts/playtest/tutorial-tour-walk.json and tutorial-resume-walk.json with real screen captures, and the panel and tool walk sets. Docs at docs/design/tutorials.md, audit at queue/audits/2026-09-12-tutorials.json, two rough edges filed p2."
    }
  ],
  "started": "2026-09-12T20:40:51.411Z",
  "completed": "2026-09-12T21:18:18.485Z"
}
