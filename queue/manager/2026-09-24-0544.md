# Manager pass 2026-09-24 05:44

## Where the app stands
Focus is `video-editing`. Since the 05:03 pass the only change is the Premiere
measurement (`0f487923`, `docs/design/video-vs-premiere.md`) and the daily
digest and triage ten minutes before this pass. No app code landed. At Next
defaults, with no switch turned on, a recording opens in the editor and the
whole edit (cut, second clip, music, dissolve, title from A to B, captions, MP4)
reaches the end. The measurement found ten gaps, and all ten lead the queue:
the ruler off when zoomed, keys needing a click first, no one-key extract, a
freeze on a long captioned timeline, and six smaller ones.

## Findings
### Objective gap
- Checked the `video-beats-premiere` key list against `TimelineKeys.swift`. J K
  L, I O, M, V, B, A, = - backslash, Command K and Shift Delete are all mapped.
  Q W, apostrophe and semicolon, S and a default transition key are not, and
  each has an open p1 task (seq 40, 60, 80, 100). No new task.
- `video-share`: MP4, GIF, HEIC and a PNG frame are all on the one Export sheet
  (`VideoExportDialog.swift`). The missing size row is
  `the-export-sheet-lets-you-pick-the-size-of-the-v`. No new task.
- Nothing measures the success line "the whole cut to ninety seconds is under
  40 presses and drags". Filed
  `count-the-ninety-second-cut-against-premiere-aga` (p1 115). It waits on the
  five fixes it would count, so it does not add to the ready count today.

### Competitors
- The Premiere report is thirty minutes old and was made from real pictures.
  No fresh look. The one thread it left open, sound while shuttling, can't be
  checked by a walk. Still an open thread.
- Moving a linked sound to another audio track without detaching it first:
  Premiere also needs Unlink first, so Photonz behaves like Premiere here.
  Closed, no action.

### Information architecture
- No change since 05:03. The tool bar and the Layers list tasks (p1 180, 240)
  still hold the two bends. The digest added a line to the keys task about who
  owns a letter key. No action.

### Workflows
- Walked the Premiere report's counted workflow. Removing a stretch is 6
  actions plus a first click today, against Premiere's 5. Trimming a head is 4
  against 2. After the keys, extract and Q/W tasks land, both should match
  Premiere. The new counting task checks that.

### Fidelity to the mocks
- No app change since the 05:03 side-by-side (`shots/2026-09-24-0500-mock-video.png`).
  No action.

### UI quality
- No app change since the last pass. No probe run: the Premiere report's
  captures are 30 minutes old and show the same build.

### Architecture
- No `Sources/` change since the last pass. No action.

### Repeated code
- None new.

### Process
- **The nine tasks the Premiere report filed had no try path and no audit line,**
  and several older video tasks were missing the follow-up bar line. Added both
  lines to 28 open video tasks ("works at Next defaults with no switch turned
  on, and an audit … gives the try steps and a real picture"; the follow-up bar
  line). Left out the investigation, the flaky test and the switch-copy tasks,
  which are not features.
- The measurement task wrote a doc, not an audit. That is right for a
  measurement. No action.
- Seven cards are still up. None has been overtaken: the panel line length
  card is still open even though tests hold panel lines to 60, because the card
  asks whether that number should be 40. Nothing withdrawn.
- `objectives-check` prints nothing.

## Tasks filed
| id | priority | epic | one line |
| --- | --- | --- | --- |
| count-the-ninety-second-cut-against-premiere-aga | p1 115 | video-beats-premiere | Once the key gaps close, count the ninety second cut in presses against Premiere |

## Staging changes
None. As at 05:03: the icon, component and redline epics sit `now` because the
user put them there, and all their tasks come after the video work.

## Questions raised
None.

## Open threads
- Sound while shuttling at 2x or backwards (Premiere plays it pitched). A walk
  can't listen. Needs a person or an audio-level check.
- The rotating check at 12:33Z saw one empty moment in 24 in the playback walk,
  and six runs on their own passed. If it shows again it becomes a p1 bug
  (logged on the standing walk task).
- The title bar says "Everything" where the mock gives name, size, length and
  saved state. This comes with saving a project.
- Four guarded force unwraps in core and render (05:20 report, 2026-09-23).
- Queue size: 47 ready against a target of six to twelve. The triage ten
  minutes ago folded what it could. The size costs claim order, not focus.

## Queue before and after
Ready tasks before: 47. After: 47 (the one filed waits on five others). App
tasks among ready: about 39 of 47.
