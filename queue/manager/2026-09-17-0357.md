# Manager pass 2026-09-17 03:57

Sixteenth pass, and the first woken by an empty queue rather than by a changed
objective. Two days ago there were 82 open tasks; there are now 11, of which 2
were claimable. The loop has been eating faster than it has been fed: 448
commits in seven days.

Eight tasks filed, seven of them app work, one card opened.

**The one thing to know: the Mac's screen has been locked since 2026-09-15
08:44, and it still is.** 83 sweeps have been asked for and none has been
served. Every audit written in the last two days says in its own rough list that
no walk could run and there is no picture of the app. That is not a fault in the
loop, which handles the lock honestly everywhere except one place, and that
place is the dashboard.

## Where the app stands

Focus is `ui-building` and its ladder is finished: every step A through E in
`docs/design/ui-building.md` is marked landed, including auto layout, variants,
shared components and styles. `next-measure.md` § 12 is delivered flag for flag,
including the pixel-scale auto-detect that doc still calls a phase-16 follow-up
(`DisplayScale.swift`, wired in `CaptureStore.swift:248`). 77 flags in
`FeatureCatalog.swift`. `Releases/Next/` still holds only
`NextExperience.swift`, so nothing is forked and the porting rule is not being
bent.

What you can try today that is new: cut a recording into pieces and drop one,
read every label of a separated screenshot back into real text in one press,
export an icon with nothing behind it, and a panel you can choose the sections
of. What nobody has SEEN is any of it.

## Findings

### Objective gap
- `video-cutting` is titled "Cutting and arranging" and only cutting exists.
  `VideoCutList` has `split`, `removePiece` and `keep`, and no way to move a
  piece; the pieces play in the order they were recorded, always. The user said
  "Start video now" on 2026-09-15 and the epic has had two tasks since. Filed as
  `put-the-pieces-of-a-recording-in-a-different-ord`, with a card, because it is
  a new gesture and because the card is also the place to ask whether video is
  where the next stretch goes.
- **Gradient paint is NOT missing**, and three passes have carried it as an open
  thread on the `icon-vector` epic note. `PathRasterizer.swift:65-83` pours a
  ramp through a path with the fill rule honoured so a gradient icon keeps its
  hole, `strokeEdge` does the same for the outline, and `ColorStyles.swift:111`
  says `.fill` and `.stroke` both accept one. Thread closed, no task. The epic
  note is stale; I left it alone because this pass only has authority to change
  stages.
- The two video slices that ARE missing are both finishing work on what exists:
  a trim handle that catches on a cut (`a-trim-handle-catches-on-a-cut-as-it-passes`)
  and the piece you picked staying visible in trim mode
  (`while-you-trim-a-recording-you-can-still-see-and`).

### Competitors
- Against Final Cut and Premiere on the one job video does today, cutting a
  recording down, the gaps are magnetic handles and rearranging. Both filed.
  Nothing else they do is worth copying into a tool whose video is a screen
  recording.
- Against CleanShot X and Shottr on redlining: nothing new. The measure spec is
  delivered and the last three passes have found no gap there.

### Information architecture
- No new finding. The double click maximise exemption list, carried four
  passes, is done (`Maximising the window on a double click belongs to Select`).

### Workflows
- Separating a screenshot and reading its labels back is now a two press
  workflow and it is good. Two things in it stop you: a pill that swallows
  clicks for six seconds afterwards (filed p1), and a count of what stayed a
  picture that you cannot get to (filed p2, behind the pill fix).

### UI quality
- Not judged on a picture this pass and it could not have been: the screen is
  locked, so the probe cannot photograph anything and I will not build the dev
  app. `an-audit-gets-a-real-picture-of-the-app-again` still sits at p1 seq 20
  waiting on the same thing.
- Worth recording because the audits get it right: a locked Mac does NOT stop
  the window being drawn, so several audits in the last two days DID force a run
  and come back with real captures. The ones with no picture say so honestly.

### Architecture
- No `@preconcurrency` anywhere in `Sources/`. Next forks no files, so nothing
  in Current is waiting to be carried across. The three provably safe force
  unwraps recorded last pass are unchanged and still bite nobody.

### Repeated code
- Nothing new this pass.

### Process
- **The dashboard is the one surface that loses the lock.** `sweep.sh status`
  prints "COULD NOT RUN: the screen was locked", `history.jsonl` records
  `screenLocked: true` on every `sweep_pass`, and the harness doc says the same.
  But `queue.mjs state` returns `sweep.last` without the field, so
  `dashboard.html:808` renders "walk sweep cut short at 0 walks 5 minutes ago ·
  83 more asked for", which is the wording for a sweep that ran out of time. A
  person glancing at the page cannot tell that two days of work has shipped
  unverified. Filed p1.
- Every finished task has an audit and the digests are daily. The follow-up bar
  is holding, arguably too well: the queue went from 82 open to 11 in two days
  and ran dry, which is the first time ordering has not been the binding
  problem.
- No card was overtaken. All six pending ones are distinct and still live.

## Tasks filed
| id | priority | epic | one line |
| --- | --- | --- | --- |
| a-notice-at-the-foot-of-the-canvas-stops-swallow | p1 seq 15 | ui-building | The pill after a separation swallows clicks across its whole capsule for six seconds |
| the-dashboard-says-when-the-loop-has-gone-blind | p1 seq 25 | unmanned-loop | The dashboard reads a locked-screen sweep as one that ran out of time |
| while-you-trim-a-recording-you-can-still-see-and | p2 seq 50 | video-cutting | Trim mode loses which piece you picked, and Delete stops working there |
| a-trim-handle-catches-on-a-cut-as-it-passes | p2 seq 60 | video-cutting | Trimming exactly to a cut is a steady hand, not a snap |
| a-label-read-back-into-text-comes-back-in-the-we | p2 seq 70 | ui-building | The page vote settles the typeface and nothing settles the weight |
| the-labels-a-reading-gave-up-on-can-be-found | p2 seq 80 | ui-building | "3 stayed pictures" and no way to get to them (deps on the pill fix) |
| a-picture-too-wide-for-the-box-it-sits-in-says-s | p2 seq 90 | ui-layout | A picture overhangs a capped box; check first whether the overflow line already fires |
| a-png-says-what-it-will-weigh-before-you-save-it | p2 seq 110 | icon-export | Only the lossy formats weigh themselves, so you cannot watch the canvas come off |
| put-the-pieces-of-a-recording-in-a-different-ord | p2 seq 120 | video-cutting | Blocked on its card: should the pieces be rearrangeable |

Seven of the eight unblocked tasks change `Sources/`. Six of the nine ready
tasks are app work; two of the other three are the p3 pair below.

### How the two p1s were reproduced
Neither could be reproduced by clicking, because no walk can run on a locked
screen. Both were reproduced in the code and, for the second, in the live queue.

The pill: `EditorView.swift:521` puts `.allowsHitTesting(action != nil)` on the
whole `canvasNoticeChip` HStack, and `CopyConfirmation.swift:165` gives any pill
with an action a 6 second life. So the whole capsule, words and padding
included, is a hit target over the middle of the canvas after every separation.

The dashboard, run at 10:52Z:
```
$ node queue/bin/queue.mjs state | ... sweep.last
{ "ended": "2026-09-17T10:52:31Z", "seconds": 15, "walks": 0,
  "passed": 0, "failed": 0, "complete": false }
$ ioreg -n Root -d1 -a | grep CGSSessionScreenIsLocked -A1
  <key>CGSSessionScreenIsLocked</key><true/>
$ queue/bin/sweep.sh status
  Last sweep 2026-09-17T10:52:31Z COULD NOT RUN: the screen was locked...
```
`screenLocked` is in the recorded sweep and in the status line, and gone from
the state the page reads.

## Staging changes
- `design-system` **next to now**. It has held two open p3 tasks for three
  passes while staged `next`, which the staging rule forbids, and neither task
  is worth demoting the focus for. It goes back to `next` when they are done.
  This is the same move `unmanned-loop` got on 2026-09-13 and for the same
  reason.
- Nothing demoted. `ui-building` keeps the focus even though its written ladder
  is finished, because five of the nine ready tasks sit under it or `ui-layout`.

## Questions raised
- `put-the-pieces-of-a-recording-in-a-different-ord-you-can-cut-a-recording-into-pie`:
  should the pieces of a cut recording be rearrangeable, or should the cutting
  that exists be finished first, or should the next stretch go somewhere other
  than video. The brief says plainly that no video work so far has been seen
  running, and that unlocking the Mac is worth more than any of the three
  answers.

## Open threads
- **The screen has been locked for 43 hours and the card that would fix it has
  been sitting unanswered since 2026-09-15.** Nothing the loop can do reaches
  it. The p1 filed this pass at least makes the dashboard say so.
- `measure-redline` is staged `now`, all its children are `later`, and it has
  held no open task for three passes. Its spec is delivered. It is not costing
  anything, but a `now` epic with nothing in it is a staging question for next
  pass: either something is missing from redlining that nobody has written down,
  or it goes back to `next`.
- The separated-text reading is not saved, so a document holding a hundred and
  forty read runs re-reads them in the background on every open, a few hundred
  milliseconds of quiet work. Recorded in
  `queue/audits/2026-09-16-separated-row-says-its-words.json`; nobody has
  noticed it yet, so it did not earn a task.
- The transparency swatch in the Export sheet sits hard right of the checkbox it
  explains. Nit, left in `queue/audits/2026-09-17-icon-export.json`.
- `rightClick` does not scroll to what it names the way `press` and `selectRow`
  do, which is why `measure-row-menu-walk` fails on unchanged HEAD. Already
  folded into `walks-that-fail-in-the-full-sweep`.
- `source` is still not a field on a task, only a phrase in the first log line.
  Seventh pass carrying it.
- The Experiments window is 77 paragraphs in file order. Sixth pass.
- A copy still cannot be pointed at a different component. Ninth pass.
- Nobody has walked a whole tutorials track as a person, or driven the panel
  from the keyboard. Blocked on the lock like everything else.

## Queue before and after
Ready tasks before: 2. After: 9. App tasks among ready: 6 of 9.
