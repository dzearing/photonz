# Manager pass 2026-09-18 01:31

Eighteenth pass, woken by a queue down to two claimable tasks. Seven filed, all
seven changing `Sources/` or the thing that tests it.

**The one thing to know: the app aborts every time you rename a layer, and has
done since 19:14 last night.** It is filed p0. It is a Swift exclusivity
violation born in `4f83fd70`, it happens on every rename in every document, and
it has left 21 crash reports on this Mac in six hours. Four sweeps saw it and
none of them said the word crash, because a walk whose app died is reported in
the same words as a walk that ran out of time. That second half is filed p1
against the loop.

This pass also read the app as it is rather than as the docs describe it: six
scripted walks were run here, on the current build, and every claim below that
says something is broken carries the command and its output.

## Where the app stands

Focus is `ui-building`. 79 flags in `FeatureCatalog.swift`, 77 on by default in
Next, so the user gets all of Next without turning anything on; the two that are
off are off on purpose. `Releases/Next/` still holds only
`NextExperience.swift`, so nothing is forked and the porting rule is not bent.

New since the 11:07 pass and untried by anyone: an Experiments window sorted
into the parts of the app its flags belong to, a find field that does not freeze
on a long list, and a reading of a separated page that is saved with the
document. That last one is also what broke renaming.

The written ladder in `docs/design/ui-building.md` is still finished, third pass
running. It did not bind this pass and it is not what emptied the queue: what
emptied the queue is that nine of the thirteen open tasks are waiting on a
decision card, and the Mac has been locked since 2026-09-15 so nobody is
answering them.

## Findings

### Objective gap
- **Renaming a layer aborts the app.** `EditorState+LayersPanel.swift:499` reads
  `readWordsForRows`, which reads `document`, from inside the closure
  `History.perform` is already holding `document` under exclusive access for.
  Swift's exclusivity check calls `abort()`. Filed p0:
  `renaming-a-layer-stops-crashing-the-app`. Reproduction below.
- `icon-vector` carries two gaps its own spec names and nobody filed. Points can
  only be gathered by Shift clicking one at a time, which on a thirty point icon
  is thirty clicks, and the Pen draws corners, bends and half-smooth points as
  the same dot while the reshaping chrome tells all three apart. Both filed
  (`points-on-a-path-can-be-swept-up-with-a-box`,
  `the-pen-s-points-say-what-kind-of-point-they-are`). No card on either: the
  first is a gesture every vector tool has had for thirty years, the second is
  the Pen borrowing chrome that already ships.
- The icon previews row still vanishes when you pick nothing, which is the same
  bug the Width row had and had fixed yesterday. Filed:
  `the-row-of-icon-previews-stays-up-when-you-pick`.

### Competitors
- Nothing new, and deliberately nothing filed. A tool whose layers list crashes
  the app on rename is not losing to CleanShot X on features. The one-move
  eyedropper carried from the last pass stays an open thread; it needs a card
  and nine are already waiting.

### Information architecture
- No new finding. The Experiments window landed its grouping at 22:52 last
  night, which closes the finding the last seven passes carried.

### Workflows
- **Rename a layer, lose the document.** Above, and it is the most common thing
  anyone does in a layers list.
- A screenshot separated on a build older than yesterday reads its whole page
  again on every open and never writes the answer down, because a reading
  deliberately does not mark the file edited and nothing else saves it. Filed
  p3: `a-screenshot-separated-before-today-only-has-to`.

### UI quality
- Not judged on a picture again, fourth pass. The screen is locked, so the probe
  cannot photograph anything a person would see, and `queue/manager/shots/`
  stays empty. The walks run under the lock ARE real captures of the window and
  several audits use them, which is why this is a gap in the manager pass rather
  than in the loop.

### Architecture
- No `@preconcurrency` in `Sources/`. Next forks nothing, so no Current change
  is waiting to be carried across.
- The p0 is worth an architectural line rather than just a bug line: the shape
  that caused it is reading a computed property off `self` inside a
  `perform { ... }` closure, and the commit that introduced it added several
  such closures. The task asks the runner to check the others for the same
  shape, because one of them crashing is proof the pattern is not safe here.

### Repeated code
- Nothing new.

### Process
- **The sweep described a crash as a slow walk, four times.** Filed p1:
  `a-walk-whose-app-died-says-the-app-died`. `Scripts/playtest-all.sh:151`
  falls back to "no done.json" whenever a walk left no error behind, which
  covers both "the app died" and "the walk never finished". Seven crashing walks
  also burn 22 minutes of every sweep waiting out the timeout.
- **The partial sweep works and is earning its keep.** It ran 249 of 526 walks
  in 71 minutes overnight and found 11 failures, which is how this pass found
  the p0. The stopgap filed at 11:07 was the right call.
- **Four sweep failures were sitting behind a decision card they have nothing to
  do with.** The four `separate-*` walks run in 5 to 7 seconds on a locked
  screen and fail on a document expectation; they were inside
  `walks-that-fail-in-the-full-sweep`, which cannot be claimed because its deps
  wait on the overnight-lock card. Pulled out as
  `a-separated-screenshot-hands-back-the-pieces-the`, cross-linked both ways.
  This is the second pass running that has had to do this, which is starting to
  look like a fault in the umbrella task rather than bad luck.
- No card is overtaken. All nine pending ones are distinct and live.
- Every finished task has an audit; digests are daily.

## Tasks filed
| id | priority | epic | one line |
| --- | --- | --- | --- |
| renaming-a-layer-stops-crashing-the-app | p0 seq 1 | ui-building | Renaming any layer aborts the app, every time, since 19:14 last night |
| a-walk-whose-app-died-says-the-app-died | p1 seq 10 | unmanned-loop | Four sweeps called a crash "no done.json" and nobody looked |
| a-separated-screenshot-hands-back-the-pieces-the | p2 seq 20 | ui-building | 173 pieces where the checks expect 151; is it the app or the checks |
| the-row-of-icon-previews-stays-up-when-you-pick | p2 seq 30 | icon-grid | The previews row disappears the moment you deselect |
| points-on-a-path-can-be-swept-up-with-a-box | p2 seq 40 | icon-vector | Thirty points means thirty Shift clicks |
| the-pen-s-points-say-what-kind-of-point-they-are | p3 seq 1360 | icon-vector | Corners, bends and half-smooth points all draw as one dot |
| a-screenshot-separated-before-today-only-has-to | p3 seq 1370 | ui-building | An old separated file reads its whole page again on every open |

All seven change `Sources/` or the harness that drives it. Eight of the nine
ready tasks are app work.

### How the p0 was reproduced
Run here at 2026-09-18 01:26 local, on HEAD `33132430`:

```
$ Scripts/playtest-all.sh --no-build unique-layer-names
unique-layer-names-walk                   186s  FAILED  no done.json
==> 0 passed, 1 failed
```

It wrote its first snapshot, then nothing, and no probe process was left alive.
A crash report appeared at the same moment, and its faulting stack is the whole
diagnosis:

```
swift_beginAccess
EditorState.history.getter
EditorState.document.getter
EditorState.readWordsForRows.getter
closure #1 in EditorState.renameLayer(id:to:)
History.perform(_:)
EditorState.renameLayer(id:to:)
LayersListView.commitRename(id:)
```

When it started, from the sweep logs: at 17:21 `unique-layer-names` was ok in
8s, `duplicate-names` ok in 10s, `shared-component` ok in 14s. At 19:15, the
first sweep after commit `4f83fd70` at 19:14:32, all three read 186-187s FAILED,
and every sweep since says the same. `~/Library/Logs/DiagnosticReports` holds
three Photonz Probe crashes across 09-12 and 09-13, then none until
2026-09-17 19:27:04, then 21 in the next six hours.

The `separate-*` cluster was reproduced separately and on demand:

```
$ Scripts/playtest-all.sh --no-build separate-boxes
separate-boxes-walk   6s  FAILED  step 16 (expect): no row called "Box 2" is in
                          the panel; the ones that are: Text 1, Box 4, Text 9,
                          Fill, Box 3, Library
```

That one is filed p2, not p1, because it is not yet known whether the app or the
check is wrong, and the task leads with settling that.

## Staging changes
`design-system` goes back to `next`. It was promoted to `now` on 2026-09-17
only because it held two open tasks while staged `next`, with a note saying it
would go back the moment they were done. Both are done (the Experiments window
grouping and the UX-PATTERNS scrub, both 2026-09-17) and it holds none.

Nothing was promoted. With nine ready tasks the loop is not idling, and the
question of what replaces the finished `ui-building` ladder is a product
question that deserves the user, not a guess: promoting a pillar's worth of
speculative work is the epic-scale version of the capture loupe. It is carried
below as the thread it has been for three passes.

## Questions raised
None. Every task filed this pass is fixing or finishing something that already
ships, so none of them needed a card, and with nine already waiting that is the
right shape for a pass.

## Open threads
- **The written plan is finished and nothing has replaced it.** Third pass. It
  did not bind this time, but it will.
- **Nine cards are waiting and the Mac has been locked for three days.** Nine of
  thirteen open tasks at the start of this pass were blocked on one, which is
  what emptied the claimable queue, not the loop working fast. A pass that can
  only file unblocked work is a pass with a narrower choice of findings.
- `walks-that-fail-in-the-full-sweep` has now had findings pulled out of it by
  hand twice in two passes, because it is an umbrella holding 101 unrelated
  failures behind a decision card. It may want splitting rather than another
  rescue.
- No one-move eyedropper for "what colour is that pixel". Needs a card.
- `queue/manager/shots/` empty for four passes.
- `source` is still not a field on a task, only a phrase in the first log line.
  Ninth pass.
- Nobody has walked a whole tutorials track as a person, or driven the panel
  from the keyboard.
- Exporting an icon at several sizes in one go is four trips through the Export
  sheet.

## Queue before and after
Ready tasks before: 2. After: 9. App tasks among ready: 8 of 9.
