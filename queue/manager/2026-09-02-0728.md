# Manager pass 2026-09-02 07:28

Third pass. The second (2026-09-02 02:40) filed nine; seven are done and pushed, two wait on cards. Since then the loop also finished the digest's follow-ups (item counts, legend under the toggle, Size mode caliper names, the head handle, the playtest harness) and produced the first real daily digest since 2026-08-23. The loop ran clean; the queue had two ready tasks when this pass began.

## Where the app stands
Focus is measure-redline. In Next with default flags the user can: capture a window by clicking it, with title and shadow (next-window-capture), see a loupe with pixel coordinates (next-capture-loupe), open the last capture from the toast's Edit row or Shift-Command-6 (next-capture-toast-edit), measure in Distance, Size, Gap and Alignment modes with a one-line hint (next-measure-modes, next-measure-align), read a guide as "Left edges, 4 items" on the row, the inspector and the chip on the picture, drag a caliper's number by its pill, caption an arrow with a pill that stays on the picture and follows a dragged arrow (next-arrow-captions), pick several rows with shift and command, and hand off picture plus spec list with one Copy Image (next-measure-panel). A scripted playtest harness now drives the probe editor from JSON and renders it offscreen.
Missing against the vision: text is not an element, so Size mode shows nothing on a heading or label and a caliper across a label parks its number on the words; an arrow caption cannot be moved by hand though a caliper's number can; the Measure tool still hands back to Select after every landing (card open). Nothing has been verified on a live screen: the probe holds no Screen Recording grant (card open). Five cards are pending and none has been answered since 2026-08-23.

## Findings
### Objective gap
- Text is not an element. Size mode reads nothing on the General heading or a row label (audit legend-under-toggle rough 3), and the 121 px caliper in caliper-head-3-size.png sits on the word "clipboard" because a label is not a subject the planner can avoid. ElementBounds throws glyph edges out on purpose (ElementBounds.swift lines 21, 55, 77), so a text line needs its own rule. Task `a-line-of-text-counts-as-an-element-size-mode-re` (p1).
- The arrow caption's pill cannot be moved (arrow-captions rough 5) while the caliper's pill became its head grab in 392ce3b; the model already has captionOffset (Layer.swift:86). Task `an-arrow-s-caption-pill-can-be-dragged-to-the-sp` (p2, epic-uehjr1).
- Every spec section 12 item still has code behind it: twelve flags in FeatureCatalog.swift, nine Next-only, all default on. No action.
- Sub-epic spec-export: the hand-off is one copy, one key, and one menu that mirrors the panel. No open gap with evidence this pass; the export-to-file twin of Copy Image is an open thread.

### Competitors
- Figma and Sketch inspect give the size of a text box; Photonz gives nothing for text. That is the text-as-element task above, and the one gap this pass names for the focus.
- Color sampling stays on its card; scrolling capture stays an open thread until window capture has been judged by the user (unchanged from the last pass).

### Information architecture
- Tool letter I picks Measure and cycles its modes; the mode is also chosen from the tool button. After a landing the tool returns to Select, so reaching Size costs two presses (walk step 4). That is the pending stay-in-hand card, not a new finding. No action.
- Measure menu and panel menu now mirror each other (43269d8) and the mirror rule is written into next-measure.md section 6. No action.
- The only dashed-caliper commands without a key are Show All and Hide All (measure-menu-parity rough 3). Open thread.

### Workflows
- Capture to spec list is walked in the redline-walk audit: eight steps, two of which the loop could not fire by chord. The hover-only Edit on the toast, the one stop it found, shipped as the Edit row (17b11b9). No new stop found by reading; the next walk should run after the stay-in-hand card resolves.

### UI quality
- Judged from the audits' offscreen renders (queue/audits/2026-09-02-*.png), not a live screen: the probe still has no grant, so no shot was saved under queue/manager/shots/. Pills, connectors and ticks are consistent; the legend and toggle now share one inset (ff5517d). One flaw seen: the 121 px pill on the label text, covered by the p1 task.
- A selected caliper's draggable pill and feet give no cursor cue (caliper-head rough 1). Task `the-grab-points-of-a-selected-caliper-show-a-han` (p3).
- Window titles in the capture pill are cut at the end (window-highlight-title rough 2). Task `a-shortened-window-title-keeps-its-ending-so-two` (p3).

### Architecture
- Force unwraps in PhotonzCore and PhotonzRender: zero. Six @unchecked Sendable sites, each with a doc comment; no @preconcurrency. Releases/Next still holds only NextExperience.swift, so every change reached Next by being shared. PhotonzRender changed twice in the week, neither on the composite path. No action.
- CanvasView.swift is 3556 lines (3430 last pass), EditorState.swift 3550, EditorView.swift 1857, LayersPanel.swift 1834. The Measure slice of the canvas view is where the p1 task and the shared placer will both land. Task `the-measure-tool-s-canvas-code-moves-out-of-the` (p3, no behavior change, pixel-identical replay of four audit walks).

### Repeated code
- Three pill placers (caption, measure readout, legend) are already the pending task `every-pill-on-the-canvas-finds-its-spot-through`, correctly gated on the two placement cards through deps. Confirmed the deps are set. No action.
- Three audits had rebuilt a throwaway harness; the shared one shipped (4e7acfc, Sources/Photonz/Playtest). No action.

### Process
- 41 tasks finished 2026-09-02. Every feature task has an audit; five small fixes shipped without one (readout pill at the layer edge, Size caliper names, dragged arrow caption, edge-map unwraps, timing tests) and each is a fix folded into an existing audit's feature, so no task.
- Rough items without a follow-up before this pass: text not detected, no hover cue, title cut at the end. All filed above. Guide chips stacking at one corner and the heading reading 1 px off a card edge stay open threads.
- The digest for 2026-09-02 is real (86ee73a), the first since 2026-08-23; the spend-limit guard shipped (9245d40). Loop status ok, zero consecutive failures.
- Five decision cards are pending and gate three tasks. The digest says the same. No task; this is the user's queue.
- objectives.json still notes alignment-checks as "Blocked on a UX decision", which resolved 2026-08-22. Notes are the user's text, so it is flagged here rather than edited.

## Tasks filed
| id | priority | epic | one line |
| --- | --- | --- | --- |
| a-line-of-text-counts-as-an-element-size-mode-re | p1 | measure-redline | Size mode reads a heading or label; a caliper across a label keeps its number off the words |
| an-arrow-s-caption-pill-can-be-dragged-to-the-sp | p2 | epic-uehjr1 | The arrow caption pill can be dragged and stays put, like the caliper's number |
| the-measure-tool-s-canvas-code-moves-out-of-the | p3 | measure-redline | Measure canvas code leaves the 3556-line canvas view, pixel-identical replays |
| the-grab-points-of-a-selected-caliper-show-a-han | p3 | measure-redline | Open and closed hand cursors on a selected caliper's pill and feet |
| a-shortened-window-title-keeps-its-ending-so-two | p3 | measure-redline | Long window titles shorten in the middle in the capture pill |

## Staging changes
none. epic-uehjr1 and measure-redline hold new tasks; alignment-checks keeps its toggles task; spec-export has no open task but its work shipped this week, so it stays now until the next walk judges it. The focus is not exhausted, so no next epic was promoted.

## Questions raised
none. The five pending cards (boxed-in number, readout travel, stay in hand, color mode, screen grant) are the bottleneck; adding a sixth would not help.

## Open threads
- Two alignment guides ending in one corner stack their chips askew (guide-item-counts rough 3); the shared placer task should absorb it.
- A heading beside a card edge reads 1 px off (guide-item-counts rough 2): true of the G, not of the eye.
- Export PNG writes only the picture while Copy Image carries the spec list too; a sidecar file is the twin, if the user wants one.
- Copy Image shows a Copied pill even for a picture-only copy (copy-image-spec-list rough 3); may be noise.
- A green arrow's caption pill goes near black (arrow-captions rough 4); still needs the user's eye.
- Show All and Hide All Measurements have no key.
- Scrolling capture: raise as a card once window capture has been judged.
- The loupe's selection line reads points only; a pixel line may matter on 2x displays.

## Queue before and after
Ready tasks before: 2. After: 7. App tasks among ready: 7.
