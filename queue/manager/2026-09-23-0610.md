# Manager pass 2026-09-23 06:10

This pass ran 50 minutes after the last one because the objectives changed. The
user added a fifth success line to transitions and motion: any value can be
keyframed from a diamond, with keys on the track, easing and bezier curves, and
copy between layers. It came with a user task at the very front of p1. The queue
is not short (25 ready), so this pass files only what that change needs.

## Where the app stands
Focus is `video-editing`. Unchanged since 05:20, except that a full-size Retina
recording now plays without blinking (`playing-a-recording-never-blinks`, done,
audit written). Every video feature still sits behind
`next-a-recording-is-a-document`, which is off at Next defaults until
`a-recording-opens-in-the-video-editor-for-real-i` (seq 20) lands. Keyed motion
exists today on the icon clock and plays once on a timed layer, but only for
seven properties, with no diamonds in the panel and no lanes on a video track.

## Findings
### Objective gap
- **The keyframe task would have been claimed next and built into the wrong
  place.** `keyframe-anything-and-see-and-shape-the-keys-on` had seq 0.62 and no
  deps, so it was first in line, ahead of the editor being on at defaults
  (seq 20), the mocks being restored (seq 30) and the timeline it draws lanes on
  (seq 50). It is also four or five runner sessions of work in one task. Split
  into three ordered slices, all kept at the user's place at the front of p1.
  The user's task stays exactly as written and closes the set. Tasks below.
- The split also removes an overlap. `move-a-layer-from-a-to-b-grow-it-shrink-it-fade`
  and the keyframe task both built "keying armed, a change makes a key". Now the
  first keyframe slice builds keying once, and move-a-layer draws those keys on
  the new track. Logged on move-a-layer.
- The model exists and must be extended, not rebuilt: `LayerMotion` /
  `MotionProperty` (`Sources/PhotonzCore/LayerMotion.swift:16`) has position,
  scale, rotation, opacity, colour, stroke width and blur, and bezier easing
  (`EasingCurve.custom`). Missing: crop, corner radius, shadow, text size,
  volume, effect amounts, and Hold. Written into the slice notes.

### Competitors
- Keyframing is Premiere's Effect Controls panel and the one thing a Premiere
  editor would reach for after cutting. The user's ask covers it. Logged on
  `measure-the-video-editor-against-premiere-and-fi` so that audit points at the
  keyframe slices instead of filing a copy.
- The user asked for Shift-K and Option-K as next and previous key. Premiere's
  own defaults may differ, and K belongs to JKL. The third slice keeps the
  user's pair and must log any collision. No card: it is the user's own ask.

### Information architecture
- One arming control, not two: panel diamond (the user's ask) and the track
  header diamond (move-a-layer's first draft) would have been two switches for
  the same thing. Folded into move-a-layer's log.

### Workflows
- No change since 05:20: a first-time user at Next defaults still lands in the
  old recording window until seq 20 lands. No action.

### UI quality
- No probe screenshots. Nothing visible changed since the user's audit this
  morning except playback, which has its own audit with window captures.

### Architecture
- The one app commit since the last pass (`48bd9f44`, playback) wrote its tests
  first, adds no force unwrap, no `@preconcurrency`, no UI import in core. No
  action.

### Repeated code
- The icon timing strip already draws lanes and curves (`MotionStripView`).
  The lanes slice is told to reuse that drawing where it fits the mock rather
  than write a second lane renderer. No separate task.

### Process
- Latest rotating check: 50 of 50 walks passed. Playback task produced its
  audit; its four rough lines are honest notes (a late frame holds the last
  good one; frames soft for a moment after zooming in). None clears the bar.
- Closed thread: `addjson` does not store `source`. Checked: nothing in
  `queue/bin` or the dashboard reads the field, and the "created (user)" log
  line records it. Nobody notices, so it no longer goes in this report. Set by
  hand on the four keyframe tasks.

## Tasks filed
| id | priority | epic | one line |
| --- | --- | --- | --- |
| every-value-in-the-panel-has-a-key-diamond | p1 seq 0.6, after seq 20 and 30 | video-transitions-motion | A key diamond beside every value that can move. A change at another moment adds a key |
| a-layer-s-track-opens-into-one-lane-per-keyed-va | p1 seq 0.61, after slice 1 and move-a-layer | video-transitions-motion | One lane per keyed value: drag, box-select, copy, delete, ease, bezier curve |
| keys-copy-between-layers-and-the-playhead-lands | p1 seq 0.615, after lanes and sound segments | video-transitions-motion | Copy and paste keys, playhead snaps to them, next and previous key, volume line |

All three carry `source: user`, because they split the user's own ask.

### Folded rather than filed
- `keyframe-anything-and-see-and-shape-the-keys-on`: depends on the third slice,
  keeps seq 0.62, closes the set with its walk.
- `move-a-layer-from-a-to-b-grow-it-shrink-it-fade`: build on slice 1's keys.
  Do not add a second arming control.
- `measure-the-video-editor-against-premiere-and-fi`: keyframing is owned
  elsewhere.

## Staging changes
None. The objectives change was a new success line under a `now` sub-epic.

## Questions raised
None. Two cards are still up (Command S, pointer and clicks). Neither has been
overtaken.

## Open threads
- 24 ready against a target of six to twelve. The p1 rebuild chain is right and
  the p2 and p3 tasks are real. The next pass should look at trimming only when
  the p1 chain has moved.
- The Command S card still assumes one clip per recording. Reframe it once the
  tracks task lands, if it is still unanswered.
- The app dying mid-walk with no crash report (three walks). Still folded into
  `walks-that-fail-in-the-full-sweep-5`.
- Four guarded force unwraps in core and render (listed in the 05:20 report).

## Queue before and after
Ready tasks before: 25. After: 24 (the user's keyframe task now waits on its
slices). App tasks among ready: 19 of 24.
