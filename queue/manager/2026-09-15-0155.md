# Manager pass 2026-09-15 01:55

Fifteenth pass. One task filed, and it is the only one that mattered: the
sweep, which is the loop's whole regression signal, has stopped telling the
truth. Everything else this pass was ordering, because the queue does not need
more tasks, it needs the right nine at the top.

## Where the app stands

Focus is `ui-building`, but the work in flight is icons and motion, which is
what the user asked for. Four top level epics are `now` and `video-editing` is
the fifth as of this pass. Fifty eight flags in
`Sources/PhotonzCore/FeatureCatalog.swift`, none off by default in Next, and
`Releases/Next/` still holds only `NextExperience.swift`, so nothing is forked
and the porting rule is not being bent.

What you can try today: draw an icon with the Pen on a 16 to 512 frame, reshape
its points, half smooth a corner, name its fill and its outline separately,
choose ends and corners, and export it as SVG. What is missing is motion: the
five slices the answered card asked for are filed at p1 and none has started.

**The one thing to know: this morning's sweep says 115 of 425 walks are broken,
and they are not.** Same source, opposite answer, nine hours apart. Details
below.

## Findings

### Objective gap
- The five motion slices from the answered card (`icon-animate`) are filed at p1
  seq 20 to 24 and are the next real feature. No gap, just not started.
- `icon-vector` promises gradient paint and nothing in the queue covers it.
  Booleans, caps and joins, thickness and outline naming are all filed, so
  nobody is blocked on it. No action, Open threads.
- The user answered "Start video now" on 2026-09-15 and `video-editing` was
  still staged `later`, holding one open task, which the staging rule forbids.
  Promoted; see Staging changes.

### Competitors
- No new finding. Against Illustrator and Figma on icons the open gaps are
  booleans, gradient paint and a grid that snaps, and the first and third are
  filed. Nothing they do that is not written down.

### Information architecture
- No new finding this pass. The double click maximise exemption list is still
  filed at p2 seq 120.

### Workflows
- Drawing an icon goes end to end. Animating one does not start at all.
- The right hand panel does not fit its own contents at ANY window size: 1052
  points wanted against 968 available, measured in the running app with three
  layers and one piece of text picked
  (`queue/audits/2026-09-15-pane-load.json`). Position and Size is below the
  bottom edge every time, and Motion is about to be added to the same panel.
  Covered by `position-and-size-stays-on-screen-when-you-pick`, which was
  filed last night at seq **2720** and is now seq **5**.

### UI quality
- Not judged on a real capture this pass, and that is itself the finding: the
  probe reports Screen Recording as granted and then fails every capture with
  `SCStreamErrorDomain -3811`, so two of the three audits written overnight have
  no photograph of the app in them and say so in their own rough lists.
  `an-audit-gets-a-real-picture-of-the-app-again` raised p2 seq 2700 to **p1 seq
  12**. An audit with no picture is the loop reporting on itself.

### Architecture
- No `@preconcurrency` anywhere in `Sources/`. Three force unwraps in
  `PhotonzCore` and `PhotonzRender`, which the repo rules forbid, but all three
  are provably safe: `StarterComponents.swift:493` sits inside `if stroke !=
  nil`, `PathEditing.swift:105` behind `best == nil ||`, and
  `TextRasterizer.swift:145` is the NSFont to CTFont bridge. No crash, so no
  task. Last pass reported zero; the count is three and none of them bite.
  Open threads.
- Next forks no files, so nothing in Current is waiting to be carried across.

### Repeated code
- Nothing new. The easing curve list exists twice across the icon and video
  mocks and `one-set-of-easing-curves-named-the-same-way-ever` covers it; moved
  from seq 2640 to seq 9 because the motion slices above it will otherwise make
  it a third copy.

### Process
- **The sweep cannot be trusted, and it is costing runner hours.** The task log
  in `walks-that-fail-in-the-full-sweep` shows a runner reproducing
  `turn-into-a-path-walk` four separate times on a stashed clean main on the
  evening of 2026-09-14, and writing up three more walks the same night, all
  inside the window where the machine started failing everything. That is a
  night of work spent proving bugs that are probably not in the app.
- **A card was opened re-asking a settled question.** "When you animate an icon,
  where should the timing appear?" went up at 08:36Z, six hours after the user
  had answered the same thing in their own words: motion belongs on "a timeline
  that is bigger than the property panel". Its option a IS that strip. Folded
  into `a-decision-card-that-no-longer-needs-an-answer-c`, which is exactly what
  that task is for. The p1 strip task is deliberately NOT blocked on it, because
  the user already said yes.
- **Eight tasks filed in the last day were stranded at seq 2640 to 2720**,
  behind sixty older p2s, so the freshest findings about the code being written
  right now were last in line. All eight re-sequenced into the front.
- **The follow-up throttle is holding and the queue is not rotting.** 82
  pending, median age 1.9 days, two tasks older than a week, 22 finished in the
  last 24 hours. That is about three and a half days of work at current
  throughput, not a pile. It is still far over the six to twelve this prompt
  asks for, and the honest lever is ordering rather than a cull; that is what
  this pass spent itself on.
- Audits are written for every finished task and digests are daily.

## Tasks filed
| id | priority | epic | one line |
| --- | --- | --- | --- |
| a-hundred-and-fifteen-walks-fail-on-code-that-pa | p1 seq 10 | unmanned-loop | The same source that passed 31 of 31 tutorial walks yesterday afternoon fails all of them tonight |

One task, and it does not change `Sources/`. 68 of the 82 ready tasks are app
work, which is 83 percent.

### How that one was reproduced
```
Scripts/playtest-all.sh appearance-list-min tutorial-mark-it-up size-limits first-run
==> 0 passed, 4 failed
```
Two of those four failures print the missing name inside the list of names that
are present: the Add Effect menu IS in the window, right size, right tooltip,
with an empty name, and `tool.arrow` is on screen while the step says nothing
carries that name.

Then, in a throwaway worktree, `eb474448`, the exact commit the 16:19 sweep
built and passed 31 of 31 tutorials on, was rebuilt and run tonight: the same four
fail identically. So it is not in the code.

Three hypotheses tested and DISPROVED, written into the task so nobody repeats
them: the path and outline commit `1ffeff8e` (its parent fails the same way),
`open -g` in `probe-app.sh` (reverted, no change), and the display going to
sleep (`caffeinate -u` then the run under `caffeinate -d -i`, no change).
Nothing in `Scripts/` or `queue/bin/` calls `caffeinate` at all.

## Other changes
- `an-audit-gets-a-real-picture-of-the-app-again`: p2 seq 2700 to **p1 seq 12**.
- `cut-a-recording-into-pieces-and-drop-the-piece-y`: p2 seq 600 to **p1 seq
  30**, its card answered "Start video now".
- `position-and-size-stays-on-screen-when-you-pick`: seq 2720 to **5**.
- Seven more of last night's follow-ups re-sequenced from the 2600s into 6 to 16.
- `walks-that-fail-in-the-full-sweep` now depends on the new task, so nobody
  triages 115 walks that are not broken.
- `a-path-you-drew-shows-you-what-you-can-do-to-it` was filed with an empty
  `epic` and showed on the dashboard under no objective while sitting first in
  the p2 queue; set to `icon-vector`.

## Staging changes
- `video-editing` **later to now**, and its first child `video-cutting` with it.
  The user answered "Start video now" to the card asking whether the loop should
  begin building video editing; the epic was still `later` and already holding
  an open task, which the staging rule forbids twice over.
- Nothing demoted. `unmanned-loop` keeps `now` and has earned it again this
  pass for the second time running.

## Questions raised
None. The one open card (the timing surface) was already up before this pass and
is a re-ask of a settled question, not a new one.

## Open threads
- **`design-system` is staged `next` and holds two p3 tasks**, which the staging
  rule forbids. Both are small and neither is worth promoting an epic for;
  `queue.mjs` has no way to move a task to another epic, so this needs either a
  hand edit or a command. Second pass carrying it.
- Gradient paint is in the `icon-vector` epic note and nothing in the queue
  covers it. Nobody is blocked; file it when the booleans land.
- Three force unwraps in `PhotonzCore` and `PhotonzRender` against a repo rule
  that says none. All three provably cannot fire, so none earned a task.
- The 2026-09-14 20:42 sweep ran walks that normally take five seconds for 900
  to 1045 seconds each. Whatever the environment fault is, it makes the app slow
  as well as unreadable, and that detail is in the filed task.
- `source` is still not a field on a task, only a phrase in the first log line.
  Sixth pass carrying it.
- The Experiments window is fifty eight paragraphs in file order. Fifth pass.
- Nobody has done a whole tutorials track start to finish as a person, and
  nobody has driven the panel from the keyboard. Given every tutorial walk is
  currently red for reasons that are not the tutorials, this one is overdue.
- A copy still cannot be pointed at a different component. Eighth pass.

## Queue before and after
Ready tasks before: 81. After: 82. App tasks among ready: 68 of 82.
