# Manager pass 2026-09-24 02:52

## Where the app stands
Focus is `video-editing`. The whole p1 rebuild chain landed overnight (19 commits
since 07:44 yesterday): tracks, a second clip, the Library shelf for clips, the
recording's sound on its own track, right-click menus, transition tiles, moves
from A to B with title presets, key diamonds and lanes, key copy, Premiere's
keys, captions that write themselves, and an editing-session walk at Next
defaults. All of it is on at Next defaults with no switch. The last sweep: 588 of
590 walks pass. The biggest thing missing is that **an edit cannot be saved**:
Save and Save As are dimmed on a video, so closing loses everything but an Export.

## Findings
### Objective gap
- **An edited video cannot be saved.** `EditorState.saveDocument` and
  `saveDocumentAs` return early for a recording document and `SaveAffordance`
  dims Save; the package format has no way to find a clip's recording again
  (`MovieRef` has no path). A Premiere editor saves a project. Filed
  `a-video-edit-can-be-saved-as-a-project-and-opene` (p1 196).
- **The Command S card was overtaken.** It asked about one trimmed recording with
  an arrow on it; since yesterday a video is many clips, music, titles and
  captions, so there is no single recording to write back. Two earlier passes
  said to reframe it once tracks landed. Withdrawn, reframed as a new card on
  `command-s-on-a-video-edit-does-what-the-user-pic` (blocked), and
  `trim-becomes-a-tool-…` now depends on both instead of being blocked on a dead
  card.
- Captions not following a cut breaks the first success line's workflow. Already
  filed by the walk runner at p1 seq 420; moved to 195, right behind the user's
  own tasks.
- Checked the other success lines: SRT export, MP4/GIF/HEIC and a frame are in
  code; every other gap has an open task (Library filling, clip line, fades,
  motion path, captions guides, A key). No new task.

### Competitors
- A dissolve blends the picture but the sound still switches at the edit point
  (`ClipTransitions.swift` is picture only; `AudioMix` never sees a transition).
  Final Cut cross-fades the sound with the picture. Filed
  `a-transition-carries-the-sound-across-the-cut-to` (p2 34).
- Silent shuttle at 2x and backwards (Premiere plays it pitched): logged on the
  Premiere comparison task, not filed.

### Information architecture
- The old recording window still exists behind a switch for 20 walks.
  `trim-becomes-a-tool-…` retires it once saving is in the editor. No new task.

### Workflows
- Walked the editing-session captures (`/tmp/photonz-playtest/an-editing-session-walk/`).
  Open, cut, add a clip, dissolve, title, captions and export all reach at
  defaults. The flow stops at the end: there is no way to keep the work except
  Export (task above).
- The `Clip added` notice sits on top of the bottom caption
  (`3-second-clip-sc.png`). Same band as the trim bar over captions and the
  notice under the trim bar: folded all three into
  `the-trim-bar-a-recording-opens-with-stays-clear` and dropped its twin
  `a-word-the-canvas-says-is-readable-when-a-trim-i`.

### UI quality
- No new probe run; looked at the walk's fresh window captures. Timeline, tracks
  and panel read as the mock. Rough: the Animating list is 15 rows on a clip
  (owned by `the-properties-pane-opens-on-the-clip-line-and-l`), `Write Ag...`
  in the Captions panel (folded into `captions-show-safe-area-…`), Library tile
  names cut in the middle (folded into `recordings-and-sounds-can-go-into-the-library-wi`).

### Architecture
- The overnight commits wrote tests first (per their audits), add no
  `@preconcurrency` and keep core free of UI imports. The four guarded force
  unwraps from the 05:20 report are unchanged. No action.

### Repeated code
- None new worth a task.

### Process
- Every one of the 17 feature commits since yesterday has an audit, and their
  rough lines went to tasks or logs. Four stray tasks titled `x` were filed by
  runners trying `addjson`; `trying-out-addjson-never-files-a-real-task` owns it
  and was re-homed from `loop-robustness` (staged later) to `unmanned-loop`.
- `the-video-guides-teach-the-editor-that-exists` was the user's task sitting at
  p2. Moved to p1 seq 192: its deps all landed, and Next has no Video track in
  Help at all until it ships.
- The video p2 follow-ups sat at seq 3170 to 3300, behind the icon and component
  p2 work. Moved them to 31 to 38 so focus work is claimed first.

## Tasks filed
| id | priority | epic | one line |
| --- | --- | --- | --- |
| a-video-edit-can-be-saved-as-a-project-and-opene | p1 196 | video-editing | Save As writes a project holding the whole edit; reopening brings it all back |
| command-s-on-a-video-edit-does-what-the-user-pic | p1 197, blocked on card | video-editing | Command S on a video does what the user picks |
| a-transition-carries-the-sound-across-the-cut-to | p2 34 | video-transitions-motion | A dissolve cross-fades the two clips' sound too |

### Folded rather than filed
- `the-trim-bar-a-recording-opens-with-stays-clear`: notices and captions in the
  same band; acceptance widened. Twin dropped.
- `captions-show-safe-area-guides-and-their-own-bar`: `Write Ag...`.
- `recordings-and-sounds-can-go-into-the-library-wi`: tile names cut.
- `measure-the-video-editor-against-premiere-and-fi`: what is already filed, and
  silent shuttle.
- `trim-becomes-a-tool-and-the-recording-window-ret`: saving split out, deps set.

## Staging changes
None. Every `now` video sub-epic still has open work.

## Questions raised
- `command-s-on-a-video-edit-does-what-the-user-pic-you-cut-a-recording-dropped-in-a`:
  what Command S does on a video edit (replaces the withdrawn 2026-09-20 card).
- Withdrawn: `trim-becomes-a-tool-and-the-recording-window-ret-you-trimmed-a-recording-and-drew`,
  overtaken by tracks.
- Still up and not overtaken: curve in lane or Graph panel, Library in the video
  dock, panel line length, Experiments description length, pointer and clicks.

## Open threads
- 41 ready against a target of six to twelve. This pass filed three and dropped
  one; the excess is runner follow-ups plus icon, component and loop p2 work
  staged `now`. The video work is ordered first, so the size costs order, not
  focus. If the next pass still sees over 30, stage `icon-svg` or `ui-building`
  back to `later` rather than dropping real defects one by one.
- Silent shuttle and reverse play: file if the Premiere comparison names it.
- Moving a linked sound segment to another audio track is not built (linked
  sound audit). Check against the success line "move any audio segment" next pass.
- Four guarded force unwraps in core and render (05:20 report).

## Queue before and after
Ready tasks before: 40. After: 41. App tasks among ready: about 34 of 41.
