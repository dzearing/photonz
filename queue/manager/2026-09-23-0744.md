# Manager pass 2026-09-23 07:44

This pass ran because objectives.json changed, not because the queue ran low
(23 ready). The change was one word: the video-editing success line now points
at video.html and its walkthroughs instead of video-shell.html. The runner that
restores the video mocks made that edit and then quit before committing. So
this pass is short, like the 06:10 one.

## Where the app stands
Focus is `video-editing`. Since 06:10 a recording opens in the full editor at
Next defaults (`71b217f7`, audit `2026-09-23-recording-opens-in-editor.json`):
the timeline, B to cut and Export are all reachable with no switch turned on.
Closing an edited recording now asks first and offers Export. Still missing:
real tracks, a second clip, the transition tiles, motion keys, captions on a
track and the mock's timeline. All of it waits on one task: the mocks restore
at p1 seq 30, whose work is done but sits in a stash.

## Findings
### Objective gap
- The whole p1 video chain waits on `the-video-mocks-are-the-user-s-again-and-d18-is`.
  The runner did the work (25 files, audit written) and then ended its turn
  while its own test run was still going. The work was stashed at `73e6f696`
  and the task went back to pending. It is the next p1 to be claimed, and its
  log tells the next runner to `git stash apply` first. No new task.
- Checked every success line of the five `now` video sub-epics against open
  tasks. Each one has an owner: tracks and snapping in `tracks-you-can-add-…`,
  keys in `premiere-s-keys-…`, right-click in `right-click-does-…`, speed as a
  dropdown in `the-video-properties-look-like-…`, press T and title presets in
  `move-a-layer-from-a-to-b-…`, sound and music in `a-clip-s-sound-is-…`,
  captions in `captions-write-themselves-…`. Export (MP4, GIF, HEIC, a frame,
  with the file size shown) is already in `VideoExportDialog`. No gap left
  without a task.

### Competitors
- No change since 06:10. `measure-the-video-editor-against-premiere-and-fi`
  still owns the Premiere comparison. It can only run once tracks and sound
  land. No action.

### Information architecture
- No action. The editor at defaults still has the old recording window behind
  a switch for its walks. `trim-becomes-a-tool-…` retires it, blocked on the
  Command S card.

### Workflows
- At Next defaults, a first-time user now gets the editor instead of the
  play-and-shorten window, which is the change this morning's audit asked for.
  The next place the flow stops: no second track to put a clip on. That is the
  p1 chain. No action.

### UI quality
- No probe screenshots. The only visible change has an audit with four
  captures of the window.

### Architecture
- `71b217f7` wrote its tests first (`FeatureDependencyTests`,
  `SaveAffordanceTests`, `TutorialVideoTrackTests`). It adds no force unwrap,
  no `@preconcurrency` and no UI import in core, and does not touch the
  renderer. No action.

### Repeated code
- `71b217f7` added a list of which switch needs which in `FeatureCatalog`.
  `a-task-is-not-done-while-its-feature-needs-a-swi` could have built a second
  one. Folded into that task's log: build the done-check on the list that
  exists.

### Process
- **A runner ended its turn to wait for its own test run.** This is the fourth
  time: 09-08 twice (the walk sweep), 09-16 and 09-23 (`Scripts/test.sh`). The
  sweep rule fixed the first form, but nothing says ending a turn ends the
  task. This time it hit the task the whole video chain waits on. Filed
  `a-runner-never-stops-to-wait-for-its-own-test-ru` (p2).
- The audit on the editor-at-defaults task has five rough lines. Four belong to
  open tasks. The fifth, "(Next)" in the close question's window name, did not
  clear the bar. All five are logged on that task.
- Rotating check at 14:36: 50 of 50 walks passed.

## Tasks filed
| id | priority | epic | one line |
| --- | --- | --- | --- |
| a-runner-never-stops-to-wait-for-its-own-test-ru | p2 seq 25 | unmanned-loop | Ending a turn ends the task: runners finish their test run in the turn, and the loop resumes one that stopped to wait |

### Folded rather than filed
- `a-task-is-not-done-while-its-feature-needs-a-swi`: build on the switch list
  `FeatureCatalog` has had since `71b217f7`.
- `a-recording-opens-in-the-video-editor-for-real-i`: where each audit rough
  line went.

## Staging changes
None.

## Questions raised
None. Two cards are still up: Command S, and the pointer and clicks card.
Neither has been overtaken.

## Open threads
- The Command S card still assumes one clip per recording. Reframe it once
  the tracks task lands, if nobody has answered it by then.
- 24 ready against a target of six to twelve. Most p1 work is waiting on its
  deps, so the p2 and p3 tasks are what a runner would pick up if a p1 stalls.
  Trim only if the video chain is still stuck at the next pass.
- The app dying mid-walk with no crash report is still folded into
  `walks-that-fail-in-the-full-sweep-5`.
- Four guarded force unwraps in core and render (listed in the 05:20 report).
- "(Next)" in window names inside dialogs: file it if the user or a second
  audit mentions it.

## Queue before and after
Ready tasks before: 23. After: 24. App tasks among ready: 18 of 24.
