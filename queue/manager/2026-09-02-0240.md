# Manager pass 2026-09-02 02:40

Second pass. The first (2026-09-01 22:46) filed ten tasks; all ten are done and pushed, each with an audit, and the loop ran clean for nine hours. This pass builds on their sixteen audits.

## Where the app stands
Focus is measure-redline. In the Next release with default flags the user can today: capture a window by clicking it, with its shadow (`next-window-capture`), see a loupe with pixel coordinates while dragging (`next-capture-loupe`), open the last capture with Shift-Command-6, measure in Distance, Size, Gap and Alignment modes with a hint naming the mode (`next-measure-modes`, `next-measure-align`), see a caliper's number step off the elements it measured, caption an arrow with a pill that stays on the picture (`next-arrow-captions`), read an alignment row as "Left edges, 3 items", rename any measurement from Properties, and copy the spec list or one measurement from the Measure menu with a Copied pill (`next-measure-panel`).
Missing against the vision: the whole flow has never been walked in one sitting, only piece by piece; several measurements cannot be picked from the list; a hand-off is still two copies; the alignment chip on the picture does not say which edge it checked; the caption field swallows the A key right after an arrow lands. Two readout-placement questions wait on the user's cards.
Not one audit this week was verified live: the loop's probe app has no Screen Recording grant. That is now a card too.

## Findings
### Objective gap
- Right after an arrow lands the caption field owns the keyboard, so A types an a into the label (audit 2026-09-02-arrow-captions rough 1); the inspector Caption field commits on Return only (arrow-caption-one-draft rough 3). Task `a-second-arrow-can-follow-the-first-without-a-st`.
- The alignment chip on the canvas reads "aligned" or "off 4 px" with no edge word (`Measure.swift:329`); only the inspector and the row name say Left edges (alignment-guide-row rough 4). An exported picture loses it. Task `an-alignment-guide-on-the-canvas-says-which-edge`.
- Every measure surface has its own audit but the flow from Shift-Command-4 to a pasted spec has never been walked end to end. Task `the-redline-flow-is-played-end-to-end-as-a-first`.
- The Size hint runs sixteen words, about 500 pt, stays 3.5 s and can sit on the legend (measure-mode-hint rough 2 to 4). Task `the-measure-mode-hint-fits-one-short-line-and-ne`.
- Every spec §12 "done when" item has code behind it; verified against `FeatureCatalog.swift` (ten flags, seven Next-only, all on) and the shipped-shape notes in `next-measure.md` §6 to §9. No action.

### Competitors
- Figma and Sketch inspect show a color beside every size; Photonz reads no color off the picture. Filed as `point-at-a-color-and-its-value-joins-the-spec`, blocked on a card, because the user asked that speculative features be confirmed first.
- The two capture gaps from the last pass (window pick, loupe) shipped. Scrolling capture remains the one CleanShot X and Shottr signature Photonz lacks that a redliner would use (long pages). Not filed: capture-side and speculative; open thread.

### Information architecture
- The Measure menu has Show All and Clear but no Hide All, while the panel menu has all four (`EditorCommands.swift:349`, `LayersPanel.swift` MeasurementsSectionAccessory). Task `hide-all-measurements-joins-the-measure-menu-so` (p3).
- The Measurements list has no shift-click or command-click; the only multi-select is the canvas marquee (spec-export-menu rough 3; `LayersPanel.swift` row tap calls `selectLayer`, command-tap selects pixels). Task `shift-click-and-command-click-pick-several-rows`.
- Capture menu and menu bar menu now agree (commit 50b72c3). Tool letters unchanged (`Tools.swift`). No action.

### Workflows
- Hand-off is Copy Image then Copy as Spec List, two commands and two pastes. `copyCompositeToClipboard` (`EditorState.swift:546`) writes PNG and TIFF only. Task `one-copy-hands-off-both-the-redlined-picture-and`, with an explicit check in real apps before shipping.
- Capture to editor is now one key each way (Shift-Command-4, Shift-Command-6); the Welcome window names both and warns about Touch Bar Macs. No action.
- Measure: I, one click for Size or Gap, three for Distance, drag for Alignment; the hint names the mode. No new stops found by reading; the playtest task will count them for real.

### UI quality
- Not verified on screen: `screencapture` from this shell fails with "could not create image from display", and the probe holds no grant. No shot under `queue/manager/shots/`. The card `the-loop-can-see-the-screen-so-audits-carry-real` asks the user whether to lift that.
- Rendered evidence in the audits is consistent (glass pills, one notice slot shared by the hint and the Copied pill). Judgment call left to the user: a green arrow now gets a near-black caption pill (arrow-captions rough 4). Open thread.

### Architecture
- Force unwraps in PhotonzCore and PhotonzRender: zero (grep this pass; e36055b removed the last two). `SelectionRegion` keeps `@unchecked Sendable` with a comment saying why. The four `@unchecked Sendable` classes in PhotonzRender are caches and the renderer, each with a doc comment. No action.
- No `Release` branching outside `Releases/`; `Releases/Next/` still holds only `NextExperience.swift`, so every change this week reached Next by being shared. No action.
- Perf: window pick 0.002 ms per move, loupe 0.2 to 0.4 ms, gap hover 4.1 ms per debug probe; nothing touched the composite path. Two ElementBounds timing tests are flaky under load; task `timing-budget-tests-pass-reliably-on-a-busy-mach` already pending. No action.
- `CanvasView.swift` is 3430 lines, `EditorState.swift` 3300, `LayersPanel.swift` 1715. Growing, not yet blocking. Open thread.

### Repeated code
- Two pill planners: `CaptionPlanner.plan` (`AnnotationEditing.swift:174`, candidates scored by rank, head zone, shaft crossing, off-canvas) and `MeasureLabelPlacement.plan` (`MeasureLabelPlacement.swift:216`, candidates scored by rank, nudge, subjects, overlaps, off-canvas, travel). Same shape, two scorers. Both pending placement decisions (boxed-in, leash) would land in the measure one only. Extraction candidate: one scored-candidate placer both feed. Not filed: no feature is blocked yet and the two decisions should resolve first. Open thread.
- The hint pill and the Copied pill already share `canvasNoticeChip`. No action.

### Process
- All ten tasks from the last pass finished with an audit; sixteen audits landed 2026-09-02. Rough items without a follow-up: four, all filed above (caption key, guide chip edge, hint width, list multi-select). Another four are folded into the playtest task's notes.
- Digests 2026-08-24 to 2026-09-01 read "Digest generation failed"; the sign-in fix (7902e7d) landed today and the next digest run at 05:02 is the test. The spend-limit twin is pending (`the-loop-treats-a-spend-limit-refusal-on-a-diges`). No new action.
- Loop health: `status.json` ok, zero consecutive failures, no parked tasks. No action.

## Tasks filed
| id | priority | epic | one line |
| --- | --- | --- | --- |
| a-second-arrow-can-follow-the-first-without-a-st | p1 | epic-uehjr1 | Starting a second arrow never leaves a stray letter in the first caption; inspector Caption commits on click-away |
| shift-click-and-command-click-pick-several-rows | p2 | spec-export | Shift-click ranges and command-click toggles in the Layers and Measurements lists |
| one-copy-hands-off-both-the-redlined-picture-and | p2 | spec-export | Copy Image carries the spec list as text beside the picture, checked in real apps first |
| an-alignment-guide-on-the-canvas-says-which-edge | p2 | alignment-checks | The guide chip reads "Left edges aligned" so the exported picture stands alone |
| the-redline-flow-is-played-end-to-end-as-a-first | p2 | measure-redline | Walk capture to hand-off as a first-time user, fix three stops, log the rest |
| the-measure-mode-hint-fits-one-short-line-and-ne | p2 | measure-redline | Every mode hint is one short line; the bracket tip moves beside the Mode control |
| hide-all-measurements-joins-the-measure-menu-so | p3 | measure-redline | The Measure menu and the panel menu offer the same commands |
| point-at-a-color-and-its-value-joins-the-spec | p3, blocked | measure-redline | A Color mode in Measure that puts hex values in the spec, if the user wants it |
| the-loop-can-see-the-screen-so-audits-carry-real | p3, blocked | unmanned-loop | Grant reporting and real screenshots in audits, once the user grants the probe screen access |

## Staging changes
none. `epic-uehjr1`, `alignment-checks` and `spec-export` each hold a new open task and stay `now`; `hover-measure` and `measurements-panel` stay `later`. The focus is not exhausted, so no `next` epic was promoted.

## Questions raised
- `point-at-a-color-and-its-value-joins-the-spec-should-the-measure-tool-read-col`: should the Measure tool read colors off the picture for the spec? Recommended: not now.
- `the-loop-can-see-the-screen-so-audits-carry-real-may-the-unmanned-loop-take-scree`: may the unmanned loop take screenshots of its own probe app? Recommended: grant.
- Still pending from the runners: the boxed-in caliper number and the readout leash.

## Open threads
- Scrolling capture for long pages: the one competitor signature a redliner would still leave Photonz for. Raise as a card once window capture has been judged.
- Size mode's 1 px border and soft-outline misses need a signal beyond the edge map; wait for the user's verdict on Size mode.
- A green arrow's caption pill goes near black so white text reads; judge whether it still reads as the arrow's color.
- The loupe and drag box read the selection in points only; a pixel line may matter on 2x displays.
- `CaptionPlanner` and `MeasureLabelPlacement` are two scorers of one idea; unify after the two placement decisions resolve.
- Readouts still cannot be dragged to a spot you prefer; build only if the automatic picks feel wrong in use.
- Window title labels are cut at the end, so titles differing only at the end read the same.
- The spend-limit task had been filed with `area: app`; corrected to `queue` this pass along with the new loop-screen task, so the dashboard's app count is honest.

## Queue before and after
Ready tasks before: 2. After: 9. App tasks among ready: 8 (the spend-limit loop task is the one process item).
