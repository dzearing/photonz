# Manager pass 2026-09-17 11:07

Seventeenth pass, woken by an empty queue for the second time in eight hours.
The 03:57 pass filed eight and the loop had eaten all but two of them by 11:00.
Eight filed here, seven of them app work, one card opened.

**The screen has now been locked since 2026-09-15 08:44, three days, and the
card that would settle it has been sitting unanswered for two.** Everything
below was read in the code rather than watched in the app, and where a finding
claims something is broken it says so out loud and is filed p2, not p1.

**The thing to take away from this pass: every `now` epic has run out of
WRITTEN work.** `docs/design/ui-building.md` is 250KB of "Landed"; its ladder
ends at step E9 and there is no F. icon-grid, icon-vector, icon-animate and
icon-export between them hold one claimable task. The loop is living entirely
on follow-ups from its own audits, which is why the queue drains to zero every
two days and why a manager pass is now a scavenging exercise. That is a
planning gap, not a loop fault, and it is the first thing the next pass should
fix.

## Where the app stands

Focus is `ui-building`. 79 flags in `FeatureCatalog.swift`, 77 of them in Next
and 77 on by default, so the user gets all of Next without turning anything on.
`Releases/Next/` still holds only `NextExperience.swift`: nothing forked, the
porting rule not bent.

New since 03:57 and untried by anyone: the Delete key drops a piece of a
recording, a trim handle catches on a cut, a page of labels comes back at one
size and one weight, an SVG says what it will weigh, and a question you
silenced can be switched back on from a new Settings window.

## Findings

### Objective gap
- **The focus has no written work left.** Above. No task: this is a staging
  question for the next pass, and the honest options are writing the next
  ladder for `ui-components`/`ui-layout` or asking the user which pillar goes
  next. Given that the user rejected shipped alignment guides for being nobody's
  idea, guessing an epic's worth of work is the wrong answer.
- The gaps the spec itself names as not built, from `ui-building.md` "What the
  first version deliberately does not do": no re-attach after detach, the shared
  shelf is one flat list that does not travel with a document, no image as a
  component knob, no token layer under styles so no light and dark resolution of
  a named colour. All real, none of them the loop's to pick unasked.
- `measure-redline` was carried last pass as a `now` epic with nothing in it.
  It has one now: the size finding below. Staying `now`.

### Competitors
- Against Shottr and CleanShot X on redlining: an eyedropper exists but only
  three steps in, behind a swatch inside the colour picker. Both of them answer
  "what colour is that pixel" in one move. Not filed, because a new tool on the
  tool bar is exactly the kind of thing that needs a card and there are seven
  cards already waiting. Open thread.
- Against Final Cut on the one video job that exists: rearranging is filed and
  blocked on its card; magnetic handles landed this morning and their one real
  cost is filed below.

### Information architecture
- **The Experiments window is 77 flags in the order somebody happened to add
  them.** Seventh pass carrying it, which is what earns it a task under the
  follow-up bar. Filed p3, not higher, because measuring it took the urgency out:
  77 of the 79 definitions are `enabledByDefaultIn: [.next]`, so nobody is
  hunting for switches to turn on, and the search field finds the one an audit
  named. `the-experiments-window-sorts-its-flags-into-the`.

### Workflows
- **Reading a page of labels has a dead end.** The line saying "3 stayed
  pictures" is the only way to those three, it lasts six seconds, and nothing
  remembers it afterwards. Getting the count back means a full second reading of
  the page. Filed: `the-labels-a-reading-gave-up-on-can-still-be-fou`.
- **A trimmed recording has an unreachable band.** The magnet that landed this
  morning means no handle can be parked within eight points of a cut by
  dragging at all. The canvas already teaches the answer: hold a key and the
  magnet lets go. Filed: `a-trim-handle-can-be-pulled-past-a-cut-when-you`.
- **Changing your mind about a component costs the whole copy.** Ninth pass
  carrying "a copy cannot be pointed at a different component" as a thread, and
  no card was ever opened against it. That is the failure being corrected here,
  not the feature. Task filed and carded.

### UI quality
- Not judged on a picture, and this time not only because of the lock: the go
  loop was live (pid 44685) and owns `.build`, so a probe build from here would
  have collided with the task it was running. `queue/manager/shots/` is empty
  for a third pass.

### Architecture
- No `@preconcurrency` in `Sources/`. Next forks nothing, so no Current change
  is waiting to be carried across. `PhotonzCore` stays clean.
- **A starter component may have gone missing from the shelf.** A walk that
  drags the Nav Bar tile out fails on unchanged HEAD. The model still has all
  five (`StarterComponents.swift`), and `maxTiles` is 60 so it is not
  truncation, which leaves the shelf's own filtering. Filed p2 as something to
  look into, because nothing here could run it:
  `every-starter-component-can-be-found-on-the-comp`.

### Repeated code
- Nothing new.

### Process
- **The sweep has filed nothing for three days, and about 224 of the 510 walks
  would run.** Reproduced below. The audit that measured the lock-safe list
  deliberately left this to the overnight-lock card, but that card asks whether
  to keep the Mac AWAKE, which is a different question from what to do while it
  is asleep. Filed p1 as the stopgap, cross-linked both ways with the task that
  is the real answer: `a-sweep-on-a-locked-mac-files-the-half-it-could`.
- **Six of eleven open tasks were blocked on cards when this pass started, and
  two of the three "pending" ones had deps on blocked tasks.** Effective ready
  was 2, both p3. The Nav Bar finding was pulled out of
  `walks-that-fail-in-the-full-sweep` for exactly this reason: it is an app bug
  with nothing to do with the lock, sitting behind the lock card.
- No card is overtaken. All seven pending ones are distinct and live, and 51 of
  58 cards in the folder have been answered, so the channel works.
- Every finished task has an audit; digests are daily.

### Not a finding, checked and dropped
- The delete-layer audit's worry that Command Delete mid-rename deletes the
  layer is **correct behaviour, not a bug**: `EditorCommands.swift:60-75` records
  it measured both ways on 2026-09-08 and again on 2026-09-17, Option Delete
  stays with the field, Command Delete goes to the menu, and Finder does the
  same while renaming a file. Logged on that task rather than filed.

## Tasks filed
| id | priority | epic | one line |
| --- | --- | --- | --- |
| a-sweep-on-a-locked-mac-files-the-half-it-could | p1 seq 10 | unmanned-loop | Three days of no regression signal at all when half the set would run |
| a-size-read-off-a-screenshot-is-the-size-the-typ | p2 seq 30 | measure-redline | A label read off a capture may say 28 px where the type was 26 |
| a-trim-handle-can-be-pulled-past-a-cut-when-you | p2 seq 40 | video-cutting | The new magnet fenced off eight points either side of every cut |
| every-starter-component-can-be-found-on-the-comp | p2 seq 50 | ui-components | The Nav Bar tile may have stopped appearing on the shelf |
| the-labels-a-reading-gave-up-on-can-still-be-fou | p2 seq 60 | ui-building | The count of what stayed a picture is gone six seconds after it appears |
| a-copy-on-the-canvas-can-be-pointed-at-a-differe | p2 seq 140 | ui-components | Blocked on its card: swapping which component a copy follows |
| what-a-reading-found-is-saved-with-the-document | p3 seq 40 | ui-building | Every open re-reads a page the app already read |
| the-experiments-window-sorts-its-flags-into-the | p3 seq 50 | ui-building | 77 flags in one list in the order they were added |

Seven of the eight change `Sources/`. Seven of the nine ready tasks are app work.

### How the p1 was reproduced
Run here at 18:03Z (11:03 local):
```
$ ioreg -n Root -d1 -a | grep -A1 CGSSessionScreenIsLocked
  <key>CGSSessionScreenIsLocked</key>
  <true/>
$ queue/bin/sweep.sh status
  Last sweep 2026-09-17T18:03:11Z COULD NOT RUN: the screen was locked, so every
  walk that looks a control up by name was refused.
  Half the set would have run and photographed the app, but half a set is not the
  state of the walk set, so nothing was filed. It runs again once the screen is
  unlocked.
  A sweep is pending; the loop runs it between tasks.
```
The last sweep that filed anything is 2026-09-15T08:15Z: 310 of 425 passed.
Everything shipped since has gone out with no whole-app check at all.

The two p2s that say something is broken were NOT reproduced here and say so in
their own notes: no walk can run on a locked screen and the live loop owns the
build directory. Both are filed p2 as things to look into, never p1, and both
lead with reproducing before changing anything.

## Staging changes
None. Every task filed sits under an epic already staged `now`, and nothing was
promoted: with nine ready tasks the loop is not idling, and promoting a pillar's
worth of speculative work without asking is the epic-scale version of the
capture loupe. The next pass inherits the question instead, stated at the top.

## Questions raised
- `a-copy-on-the-canvas-can-be-pointed-at-a-differe-you-put-a-button-on-a-screen-and`:
  should a copy already on the canvas be pointable at a different component, as
  a row in the panel, as a drop on top of it, as both, or not at all. Option D
  declines and retires the task. Brief written.

## Open threads
- **The written plan is finished and nothing has replaced it.** Top of this
  report. Next pass either writes the next ladder or asks.
- **The Mac has been locked for three days** and the only thing that reaches it
  is the user. Second pass carrying this.
- No one-move eyedropper for "what colour is that pixel", which is table stakes
  in Shottr and CleanShot X. Needs a card; not opened, because seven are already
  waiting.
- Exporting an icon at several sizes in one go is four trips through the Export
  sheet. Probably not what the user meant when they asked for SVG export, since
  SVG has no size. Worth one question some day, not this pass.
- A capped card with a picture spilling out of it says so only in the Layout
  section of the inspector; with the panel shut it says nothing. A mark on the
  canvas would be a new idiom and belongs on a card.
- `source` is still not a field on a task, only a phrase in the first log line.
  Eighth pass.
- Nobody has walked a whole tutorials track as a person, or driven the panel
  from the keyboard.
- `queue/manager/shots/` has been empty for three passes. Worth running the
  probe from a pass that starts while the loop is between tasks.

## Queue before and after
Ready tasks before: 2. After: 9. App tasks among ready: 7 of 9.
