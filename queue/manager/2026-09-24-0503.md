# Manager pass 2026-09-24 05:03

## Where the app stands
Focus is `video-editing`. Two commits since the 02:52 pass: panels keep their
margins and a walk measures it (`d3768951`), and every `now` epic now has a
success list, with the focus brief generated from `objectives.json`
(`9ba89574`). This is the first pass measured against those sub-epic success
lists. At Next defaults, with no switch turned on, you can open a recording,
cut it, add a clip, dissolve, key a title from A to B, let captions write
themselves and export an MP4. Still missing: saving the edit, and a dock and
tool bar that look like video.html.

## Findings
### Objective gap
- Checked every line of the five `now` sub-epic success lists against the code.
  Tracks (add, rename, group, collapse, hide, mute, solo, lock) are in
  `EditorState+Tracks.swift`. Markers, J/K/L, I/O, ripple delete and B are in
  `TimelineKeys`/`TimelineMarks`. Keys cover transform, opacity, blur, shadow,
  glow, text size, colour and volume (`PropertyKeys`, `MotionProperty`). A title's
  stroke is a border effect that outlines the letters. Export is one sheet. Every
  gap left already has an open task. No new task.
- A linked sound segment still moves only along time, not to another audio
  track (linked-sound audit). Detach first, then move, meets "move any audio
  segment". Open thread.

### Competitors
- Premiere and Final Cut people bring in media with Command I. Photonz has no
  import menu row: a second clip comes in by a Finder drag or through the
  Library, which is folded by default. Add Sound… takes only a movie's sound.
  The mock's File menu has "Import media ⇧⌘I". Folded into
  `recordings-and-sounds-can-go-into-the-library-wi` with a new acceptance line.

### Information architecture
- **The video dock lists every clip twice.** On a recording, Layers (Captions,
  b-roll, Sample Talk, Canvas) takes the top third of the dock above the clip's
  settings. The user's restored video.html says "NO LAYERS GROUP IN THE VIDEO
  LENS … the timeline is the layer list" (line 468). Filed
  `on-a-video-the-timeline-is-the-layer-list-and-th` (p1 365). It also frees the
  room `the-panel-keeps-its-promise-on-a-video-document` is short of (logged there).
- modes.md promises "a recording opens in Video", but the mode is one setting
  for the whole app, so doing that would re-fold every other window. The tool bar
  task ties the video tool set to the document having time instead, as the
  timeline is. Noted in its notes; no separate task.

### Workflows
- Walked the 04:44 `an-editing-session-walk` captures at Next defaults. Open,
  cut, second clip, picker, dissolve, title, captions and export all reach. Where
  the screen does not say what to do next: bringing in the second clip (no menu
  row, see Competitors) and keeping the work (`a-video-edit-can-be-saved-as-a-project-and-opene`, p1 196).

### Fidelity to the mocks
- Rendered video.html from the dashboard (`shots/2026-09-24-0500-mock-video.png`)
  beside `5-dissolve-sc.png`.
- **The tool bar is the picture tool bar.** Sixteen buttons and no Blade, where
  the mock and UX-PATTERNS D4 give video Select, Blade, Title, Shape and Measure,
  with the rest under More. Filed `on-a-video-the-tool-bar-holds-the-video-tools-an`
  (p1 415). The app has no Hand or Zoom tool anywhere, so the task leaves those two out.
- The dock without Layers: see above.
- Properties clip line and the Animating count: owned by
  `the-properties-pane-opens-on-the-clip-line-and-l`. Its wording "under Layers"
  is corrected in its log.
- The mock's title bar ("launch-teaser · 1920 x 1080 · 0:15 · saved") against the
  app's mode chip: the chip is shared chrome on every document. Open thread.

### UI quality
- The timeline bar's Easing menu reads "Ease In and..." at full window width,
  where the mock reads "Ease in out" whole. Folded into
  `the-captions-section-says-every-button-and-colou` (same fault: cut labels in
  the video chrome), with an acceptance line added. Moved that task from seq 3310
  to 39 so it is claimed with the other video p2 work, not after the icon work.
- The transition section's orange line "Both sides are the same frames, so this
  shows nothing" is a true warning about the walk's jump cut, not model
  explanation. No action.

### Architecture
- The two commits since the last pass touch core with tests (`PanelMarginRule`,
  objectives drill) and add no `@preconcurrency` and no UI import in core. No action.

### Repeated code
- The tool fold the new tool bar task needs is the one
  `a-mode-is-written-down-so-pixel-art-can-be-added` needs by mode. The task says
  to build it in core once, and the pixel art task's log says to reuse it.

### Process
- Both commits since 02:52 have audits (`2026-09-24-panel-margins.json`; the
  objectives work is loop-only). Their rough lines went to tasks or logs.
- Rotating checks at 11:35Z and 11:55Z found only the two walks the sweep task
  already owns (`keys-copy-between-layers-walk`, `panel-grip-walk`).
- `objectives-check` prints nothing.

## Tasks filed
| id | priority | epic | one line |
| --- | --- | --- | --- |
| on-a-video-the-timeline-is-the-layer-list-and-th | p1 365 | video-editing | No Layers list in a video's dock while the timeline shows; the panel opens on the picked clip |
| on-a-video-the-tool-bar-holds-the-video-tools-an | p1 415 | video-editing | A document with time gets the mock's tool bar: Select, Blade, Title, Shape, Measure, rest under More |

### Folded rather than filed
- `recordings-and-sounds-can-go-into-the-library-wi`: File > Import Media… ⇧⌘I (acceptance line added).
- `the-captions-section-says-every-button-and-colou`: Easing menu cut short (acceptance line added, seq 3310 to 39).
- `the-panel-keeps-its-promise-on-a-video-document`: re-measure once Layers leaves the video dock.
- `the-properties-pane-opens-on-the-clip-line-and-l`: "under Layers" becomes "first in the panel".
- `trim-becomes-a-tool-and-the-recording-window-ret`: where Trim sits once the video tool bar lands.
- `a-mode-is-written-down-so-pixel-art-can-be-added`: reuse the tool fold.

## Staging changes
None. The 02:52 pass suggested sending `icon-svg` or `ui-building` back to
`later` if the queue stayed over 30. Not done: the user put both in `now` from the
dashboard, the staging authority in this prompt covers the focus's own
sub-epics, and every one of their tasks already sits behind all the video work
in claim order, so demoting them would change the count but not what gets built
next. If the user wants the count down, that is their call to make on the
Objectives tab.

## Questions raised
None. Six cards are up (curve location, pointer and clicks, Library in the
video dock, Command S, Experiments switch copy, panel line length). None has been
overtaken.

## Open threads
- Moving a linked sound segment to another audio track, without detaching first.
- The mock's title bar reads name, size, length and saved state; the app shows the mode chip. Shared chrome, so any change needs the image lens too.
- The blink check cannot see a frame the window draws late (editing-session audit rough).
- Four guarded force unwraps in core and render (05:20 report, 2026-09-23).
- Queue size: 43 ready. See Staging changes.

## Queue before and after
Ready tasks before: 41. After: 43. App tasks among ready: about 35 of 43.
