{
  "phase": 7,
  "title": "Polish: liquid glass everywhere, animations, signing, perf pass",
  "status": "in_progress",
  "tasks": [
    { "id": "7.1", "title": "Liquid glass audit: toolbar, panels, popovers in GlassEffectContainer; dark/light tuned", "status": "done", "notes": "Glass clusters (toolbar, history panel, layers panel) each wrapped in a tight GlassEffectContainer. Canvas surround is now appearance-adaptive (underPageBackgroundColor) instead of forced black; fixed pre-existing empty-state bug where the ZStack hugged the toolbar width and painted a visible background column. Style popover now uses the system popover chrome (killed the phase-3 halo); width dots sit on a faint quaternary track ring; white swatch rings became .primary.opacity for light mode. Verified with real-app screenshots in dark+light (NSRequiresAquaSystemAppearance toggle) and the /tmp/photonz-style-harness showcase rebuilt against current sources." },
    { "id": "7.2", "title": "Micro-animations: tool transitions, layer add/remove, callout placement spring", "status": "done", "notes": "Toolbar: accent circle slides between tools via matchedGeometryEffect; style button and crop options scale/fade in with the capsule resizing on one spring (animation on activeTool + selectedLayerID). Layers panel rows animate add/remove/reorder (animation on panelLayers ids). Callout placement flight converted from easeOut to CASpringAnimation(perceptualDuration: 0.45, bounce: 0.25) on position/bounds/cornerRadius with easeOut fades for chrome. Verified: full style harness (31 checks) re-run green; new callout harness (/tmp/photonz-p7/ch) asserts spring runs, settles without stuck animations, and tears down to the live composite (undo clears red border pixels)." },
    { "id": "7.3", "title": "App icon refinement + About panel + onboarding empty-state", "status": "done", "notes": "Icon: baked drop shadow + glyph lift shadow, tamer sheen (0.35→0.18) so the violet carries, aperture blades clipped inside the ring, hairline rim highlight; regenerated AppIcon.icns. About: CommandGroup(replacing: .appInfo) opens the standard panel with credits (tagline + site link); NSHumanReadableCopyright added to Info.plist in build-app.sh. Empty state: glass onboarding card with three actionable rows (Open ⌘O, Capture ⇧⌘4, Paste ⌘V — paste opens a document when none exists). Verified via screenshots of the rebuilt app; About panel is the stock AppKit path, build-verified only." },
    { "id": "7.4", "title": "Perf pass against budget (16ms/12MP/10 layers); dirty-rect rendering if needed", "status": "pending" },
    { "id": "7.5", "title": "Developer ID signing + notarization in release.yml (needs Apple Developer secrets)", "status": "pending" },
    { "id": "7.6", "title": "Keyboard shortcut audit + menu completeness (matches macOS conventions)", "status": "pending" }
  ]
}
