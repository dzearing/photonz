{
  "phase": 16,
  "title": "Inspect & Measure: dimension/measure tool, edge-map smart snapping, alignment guides",
  "status": "in_progress",
  "goal": "The user's PRIMARY workflow: screenshotting UX and redlining it — measuring gaps/sizes, checking alignment, calling out spacing inconsistencies. Build a designer's redline kit on the existing non-destructive annotation model: a measure/dimension tool whose pixel sizes are auto-labeled (toggleable, restyleable, per-layer), plus SMART SNAPPING that magnetizes measure endpoints and alignment guides to UI-element edges detected in the screenshot bitmap (there is NO UI tree — snapping comes from image edge analysis). Auto-inconsistency detection is a timeboxed spike after the core lands. Sequenced AHEAD of phases 14/15 because it is the main use case.",
  "decisions": {
    "dated": "2026-06-28",
    "units": "Readouts default to LOGICAL POINTS, not raw bitmap pixels (a 2x Retina capture would otherwise read 200 for a 100pt gap). Document gains a `pixelScale` (CGFloat, default 1). Capture sets it from `NSScreen.backingScaleFactor` (available in CaptureCenter.captureRect(screen:)); opened files default to 2 for Retina-origin screenshots with a per-document override. Measures STORE the raw pixel distance (document coords == image pixels, confirmed `canvasSize = ref.pixelSize` in Document.swift:23) and DISPLAY distance / pixelScale. Unit toggle (pt | px) is per-document; decimals configurable.",
    "snapping": "There is no semantic UI tree — a screenshot is pixels. 'Snap to the edge of a UI element' is delivered via IMAGE EDGE ANALYSIS: a one-time-per-image edge map (CIEdges/Sobel in PhotonzRender) projected onto X and Y axes; the projection PEAKS are the strong vertical/horizontal boundaries and become snap candidates. For clean UX screenshots the contrast edges ARE the element edges. The existing `Snapping` enum only magnetizes a layer frame to canvas edges/center and is NOT reused for content snapping — new EdgeSnapping consumes the edge map.",
    "model": "Measure is a new annotation kind on the existing two-endpoint pattern (mirror line/arrow: endpoints + AnnotationEditing endpoint-remap on resize). The numeric LABEL is part of the layer — toggleable + restyleable, per the user's explicit ask ('toggleable as they are layers which can be configured'). Alignment guides are a separate lightweight persistent-line layer.",
    "sequencing": "16.1->16.2->16.3 deliver the measure tool (the anchor). 16.4->16.5 deliver the edge map + smart snapping (the killer combo with measure). 16.6 adds alignment guides. 16.7 is the auto-inspect SPIKE, only after 16.1-16.6 prove the edge map is reliable, with a simpler cross-highlight fallback if full auto-detection is flaky."
  },
  "tasks": [
    { "id": "16.1", "title": "Measurement model + dimension geometry (core, TDD)", "status": "done", "notes": "DONE (490 tests green, +19). New `Sources/PhotonzCore/Measure.swift`: MeasureMode {free|horizontal|vertical}, MeasureUnit {points|pixels}, MeasureCapStyle {ticks|arrows}, MeasureSegment, MeasureGeometry, MeasureContent (start/end + strokeWidth/colorHex/showLabel/unit/decimals/capStyle), MeasureBuilder (layer/updating/resized/restyled, mirrors AnnotationBuilder). Distance: free=hypot, H/V=abs(dx/dy); displayDistance divides by pixelScale for points (0->1 guard), raw for pixels; label() = '%.<decimals>f <suffix>'. Geometry: free=bare segment; H/V level the dimension line onto the END point's axis and drop a witness line from an off-axis START. NEW LayerContent.measure case threaded through: Layer.measure accessor + measureEndpoint + contains (hit near dim+witness lines) + hasEndpointHandles; AnnotationEditing.allowsFrameResize/resized routing; Document.pixelScale (default 1, custom Decodable for legacy back-compat). DocumentRenderer has a NO-OP .measure stub (returns nil) until 16.2. Tests: MeasureTests.swift (distance/units/geometry/builder/pixelScale round-trip+legacy). NOTE: H/V level onto END point's axis (drag-end = where the dimension line sits) — 16.3 drag UX should honor that." },
    { "id": "16.2", "title": "Dimension rasterizer (render, pixel-tested)", "status": "pending", "notes": "Draw the measure: main line + end caps (ticks or arrowheads), witness lines for locked modes, and the numeric label via TextRasterizer at the midpoint with a legible plate/halo. Suppress the label when toggled off; thread label visibility + style through the raster cache variant (see the phase-10/13 text-border variant precedent). Pixel test: ink along the line, witness lines present in H/V mode, label glyphs present/absent per toggle." },
    { "id": "16.3", "title": "Measure tool UX (app)", "status": "pending", "notes": "Tool enum entry + toolbar button + shortcut (M?); drag-to-create with a LIVE label preview during the drag (mirror the annotation drag-preview hold-until-composite pattern from 3.2 so there's no flash). Modifier toggles H/V lock mid-drag. Restyle via the existing style popover + a new Measure inspector section: unit pt|px, decimals, show/hide label, cap style, color. Per-document pixelScale override lives here too." },
    { "id": "16.4", "title": "Edge-map analysis (render + core, TDD)", "status": "pending", "notes": "Build a per-image edge map from the base image: gradient/edge pass (CIEdges or Sobel via CIConvolution) in PhotonzRender, then PROJECT magnitude onto X and Y -> 1-D profiles whose thresholded peaks are the strong vertical (X) and horizontal (Y) boundaries. Core owns the peak-finding + EdgeMap model (sorted candidate x's and y's with strengths); render owns the CI pass that produces the projection buffers. Cache per ImageRef. TESTS FIRST (core, synthetic): white rect on black -> detected x-edges at rect L/R, y-edges at T/B; noise below threshold rejected; nearby peaks deduped." },
    { "id": "16.5", "title": "Smart snapping: endpoints + guides magnetize to detected edges (core + app)", "status": "pending", "notes": "New EdgeSnapping (core): given a dragged point, the EdgeMap, zoom, and a screen tolerance (~6pt), snap X to the nearest in-tolerance vertical edge and Y to the nearest horizontal edge; return snapped point + which guide(s) captured (for highlight). App: wire into measure-endpoint drags (and 16.6 guides); render a highlight on the snapped edge while held. Optionally also snap to existing annotation/measure edges. TESTS FIRST: snaps to nearest peak within tolerance, no-op beyond tolerance, picks nearest of two, tolerance scales with zoom." },
    { "id": "16.6", "title": "Alignment guides: draggable snapping rulers (core + app)", "status": "pending", "notes": "Persistent H/V guide lines dropped on the canvas, draggable, snapping to edges via 16.5; toggleable + configurable (color, on/off) as layers. Serves 'draw lines on the edges of text to check alignment.' Core: guide model + axis position clamp to canvas. App: drop/drag/delete, ruler affordance, render. Mostly app; keep position/clamp math in core with a couple of tests." },
    { "id": "16.7", "title": "Auto-inspect spike: flag spacing inconsistencies (after 16.1-16.6)", "status": "pending", "notes": "TIMEBOXED EXPLORATION, only once the edge map proves reliable. From the EdgeMap, find the dominant bands/columns, measure gaps between consecutive edges, take the mode, and FLAG outliers (16,16,16,17 -> highlight the 17 + delta). Scope via marquee. If full auto-detection is flaky, ship the simpler fallback: a row of gap-measures that cross-highlight mismatched values. Explicitly research-y; capture findings in notes regardless of how far it gets." }
  ]
}
