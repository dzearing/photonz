{
  "phase": 6,
  "title": "Layers UX: panel, promote-to-layer, effects, persistence",
  "status": "done",
  "tasks": [
    { "id": "6.1", "title": "Layers panel: thumbnails, visibility, lock, opacity slider, drag-reorder, rename", "status": "done", "notes": "LayersPanel.swift (glass panel, top-right). Core: Document.moveLayers(visualSources:visualDestination:) maps SwiftUI onMove → reversed layers; Layer.duplicated/Document.duplicateLayer. Renderer: thumbnail(for:in:store:maxDimension:) = full sprite + CG downscale, cached in AppState by layer.hashValue. Style preview/commit gesture pattern in AppState (stylePreview) gives one undo step per slider drag — reused by 6.3. Layer menu: ⌘D duplicate, ⌘⌫ delete; ⌥⌘L toggles panel." },
    { "id": "6.2", "title": "Promote selection to layer (⌘J) using promoteRegionToLayer + renderer rasterize", "status": "done", "notes": "DocumentRenderer.rasterize(region:of:store:) — render + CGImage.cropping (shared top-left origin). AppState.promoteSelectionToLayer clears the marquee and selects the new layer. Layer menu ⌘J, disabled without a selection." },
    { "id": "6.3", "title": "Effects inspector: blur, shadow, border, corner radius with live preview, one undo step per gesture", "status": "done", "notes": "LayerInspector in LayersPanel.swift: opacity/blur/corner-radius/border sliders + ColorPicker, shadow toggle + blur/opacity sliders. Sliders use the previewLayerStyle/commitLayerStyle gesture infra (one undo per drag); toggles/color use setLayerStyle (one-shot). Color.hexString helper added." },
    { "id": "6.4", "title": "Blur-behind recipe as a one-click action (promote→blur→promote→crop)", "status": "done", "notes": "Document.blurBehind(selection:rasterized:blurRadius:) — ONE full-canvas raster shared by both layers: blurred backdrop (style.blurRadius) + sharp focus layer via cropContent(to: selection). AppState.blurBehindSelection, Layer menu ⇧⌘B. Focus layer selected after, marquee cleared." },
    { "id": "6.5", "title": "Copy/paste layers (within app + paste image from system clipboard as layer)", "status": "done", "notes": "Core: LayerTransfer (layer JSON + PNG bytes for image layers, pasteboard type com.photonz.layer) + PastePlacement.frame (centered, aspect-fit if oversized). Render: ImageCodec (png/jpeg/heic encode/decode — also serves 6.7). App: CommandGroup(replacing: .pasteboard) forwards ⌘C/⌘V to a focused NSTextView first so inline text editing keeps system copy/paste; paste with no document opens the image as a capture." },
    { "id": "6.6", "title": ".photonz package save/open (document.json + images/*.heic)", "status": "done", "notes": "PackageIO (PhotonzRender): atomic write via temp staging + replaceItemAt; images keyed by ref UUID, registered back under original refs via ImageStore.register(_:as:). App: documentURL tracking, ⌘S/⇧⌘S, openImage branches on .photonz extension (covers drag-drop too). UTI com.photonz.document (package) declared in build-app.sh Info.plist. Gotcha: dev builds via plain `swift build` lack the plist, so Finder/file-importer UTI mapping only works in the bundled app." },
    { "id": "6.7", "title": "Export: PNG/JPEG/HEIC at 1x/2x, copy composite to clipboard (⇧⌘C)", "status": "done", "notes": "DocumentRenderer.render(_:store:scale:) — composite assembled once (new private compositeImage), Lanczos-scaled GPU-side for 2x. ExportDialog sheet (⌘E), ⇧⌘C copies PNG+TIFF. Also added .onOpenURL in EditorView so Finder double-click / `open -a Photonz file` works (verified end-to-end with a screenshot: doc opens, layers panel renders). Note: both windows of the WindowGroup share one AppState — multi-window is effectively single-document; revisit if multi-doc ever matters." }
  ]
}
